## 2026-08-10 (27ª parte) — Pacote coletor para PC

- `collector/pack-for-pc.sh` gera `storage/rank-collector-pack/rank-collector-pc.zip` (+ `collector.env` gitignored).
- FPM: `opcache.validate_timestamps=1` (workers reciclados); reload sudo ainda recomendado.
- `RANK_COLLECTOR_LOCAL=true`; assignments OK.
- Próximo do usuário: baixar o zip no PC residencial e rodar 1× + agendar.

---

## 2026-08-10 (26ª parte) — Pós-merge GO: coletor local ligado

- `RANK_COLLECTOR_LOCAL=true` no `.env` (não commitado); assignments HTTP 200; dry-run coletor OK.
- FPM reload: **bloqueado** (sudo pede senha) — rode: `sudo systemctl reload php8.4-fpm`
- Push origin: **bloqueado** (`Permission denied (publickey)`) — precisa deploy key/SSH.
- AWA OAuth: ainda ação do usuário em Contas → Conectar Nova Conta.
- `ML_WRITE_AUTOMATION=false` intacto.

---

## 2026-08-10 (25ª parte) — MERGE Onda 4.1 em master

Autorização: usuário **GO**.
Branch `fix/onda4-1-rank` FF-merge em `master` (tip código `1b676bd7`; docs tip `bf5d661e`).
Branch **preservada** (apontando para master).

- PHPUnit Unit: **4452 / 0 falhas** (27 skipped)
- `ML_WRITE_AUTOMATION=false` intacto
- Workers: `bin/deploy.sh --workers`
- Pós-merge UI: Posição **DISPONÍVEL** `trends_partial`; QA **APROVADO**; fita "trends parcial"
- Coletor local: `RANK_COLLECTOR_LOCAL=false` → assignments HTTP 503 (esperado)
- Cron demand-only 05:20 ativo
- Sem push origin (SSH key ausente / H2)
- FPM reload: precisa `sudo systemctl reload php8.4-fpm` (senha)

---

## 2026-08-10 (24ª parte) — Onda 4.1 verificação UI + QA Pregão

Branch `fix/onda4-1-rank`. **NÃO mergear.**
- Pregão UI: Posição **DISPONÍVEL** (após limpar smoke → `trends_partial`); QA **APROVADO**.
- T3 browser: `/auth/authorize` → `auth.mercadolibre.com` PKCE (OAuth não completado).
- Governança: QA OK; kill switch OFF; 2/7.
- JS unit 31/31. `ML_WRITE_AUTOMATION=false`.
- Evidência: `docs/ops/evidence/onda4-1-ui-verify-20260810.json`.

---

## 2026-08-10 (23ª parte) — Onda 4.1 rank tracker unblock

Branch: `fix/onda4-1-rank` (a partir de master pós-Onda4). **NÃO mergear em master.**
Backup: `storage/backups/pre-onda4-1-rank-20260810_053111.sql.gz` (gzip OK, Dump completed).
Commits: `088e86de` + `9a8c8380` (demand-only cron + freshness).
PHPUnit Unit: **4452 / 0 falhas** (27 skipped). `ML_WRITE_AUTOMATION=false` intacto.

### T1a — busca autenticada (falhou — evidência obrigatória)
Egress `72.62.14.91`. Token renovado; `/users/me` OK.
`GET /sites/MLB/search` com Bearer user-scoped → **HTTP 403 forbidden** (igual público/anônimo).
Sem scraping / sem bypass de ASN. Doc: `docs/ops/RANK_TRACKER.md` +
`docs/ops/evidence/onda4-1-rank-20260810.json`.

### T1b — coletor local residencial
`collector/rank-collector.php` (<200 linhas) + README + install Linux/Windows.
Endpoints `/api/rank/assignments` + `/api/rank/ingest` (chave dedicada + HMAC; CSRF exempt).
Dry-run OK (30 kws). Ingest curl HTTP 200 → `rank_history` `position_source=proxy`.
Flag `RANK_COLLECTOR_LOCAL=false` (degradado elegante; chave no `.env` local não commitada).

### T1c — trends/highlights (sempre)
AUTH `/trends/MLB` 200 (50) · `/highlights/MLB/category/{id}` 200 (20).
`collectDemandSignals` + cron diário `--demand-only` 05:20.
Snapshot Pregão: Posição orgânica **available=true** source=`collector_local`
(observed_at preenchido).

### T2 — checklist QA
Causa: lia tabela inexistente `pregao_qa_runs`. Agora mesma fonte do Pregão:
`pregao_events` type=`qa.status` (+ receipt Redis se houver).
Valor: `OK · última execução 2026-08-10T01:28:10.999Z` (pass=true).

### T3 — Adicionar conta AWA
UI: `/dashboard/accounts` + Configurações botão **Conectar Nova Conta** → `/auth/authorize`.
`getAuthUrl`: host `auth.mercadolibre.com`, PKCE, redirect `eskill.com.br`.
Não completar OAuth (ação do usuário). Doc: `docs/ops/AWA_MOTOS_CONNECT.md`.

---

## 2026-08-10 (22ª parte) — MERGE Onda 4 em master

Autorização: verificação UI/CLI do usuário → Proceed.
Branch `feat/onda4-loop` FF-merge em `master` (tip `c94e0c8d`).
Inclui também `de4be68a` (hotfix CMV/backups) que ainda não estava em master.

- Backup: `storage/backups/pre-merge-onda4-20260810_044003.sql` (5.2G)
- PHPUnit Unit: **4449 / 0 falhas** (27 skipped)
- `ML_WRITE_AUTOMATION=false` intacto
- Workers: `bin/deploy.sh --workers`
- Branch `feat/onda4-loop` **preservada** (não deletada)
- Sem push origin (SSH key ainda ausente / H2)

---

## 2026-08-10 (21ª parte) — Onda 4: loop de otimização (dry-run / fail-closed)

Branch: `feat/onda4-loop` (a partir de `fix/pos-merge-hotfix` / `de4be68a`).
**NÃO mergeada em master.** Backup: `storage/backups/pre-onda4-20260810_040846.sql`.
PHPUnit Unit: **4449 / 0 falhas** (+15 novos; 27 skipped pré-existentes).
`ML_WRITE_AUTOMATION=false` intacto; demo dry-run `api_called=no`.

### T0 — Adicionar conta OAuth
Íntegro: `/dashboard/accounts` + seletor → `/auth/authorize` → redirect ML (PKCE).
Sem completar autorização (ação do usuário). Doc: `docs/ops/AWA_MOTOS_CONNECT.md`.

### T1 — Rank tracker
**Causa da desativação (não é scraping HTML):** `GET /sites/MLB/search` retorna
**403 forbidden** neste host (IP datacenter / CF PolicyAgent). Flag
`RANK_TRACKER_ENABLED=false` + comentários em `MercadoLivreClient`/`config/pregao.php`.
Implementado `RankTrackerService` + `rank_history` / `item_rank_keywords` / circuit /
rate limit / cache diário + `bin/rank-tracker-collect.php` (04–06h).
Evidência CLI (`RANK_TRACKER_ENABLED=true --force`): 3 keywords com
`error=search_forbidden`, circuit open, `rate_limit_per_min=6`. Sem posições inventadas.
Doc: `docs/ops/RANK_TRACKER.md`.

### T2 — SEO KPI
`SeoKpiService` + tabela `seo_interventions` + aba **Resultados** no SEO Killer
(empty state + lista). Baseline demo MLB4435358255 capturado.

### T3 — MLWriteGateway
Guardas: kill_switch → action_flag → sentinela/TRAVADA → allowlist → rate → dry-run → audit.
Painel `/dashboard/write-governance`. Demo pause MORTO MLB7346643828 bloqueada por
`kill_switch`; auditoria id 1–2. Legacy `updateItem` ainda existe (pré-Onda4) —
portão novo é o único caminho para automação futura (Onda 5 migra).

### T4 — Ads readiness
Fila real: 14 itens, waste≈R$51,89; MLB6526483148 ACOS 56,61; Executar disabled.

### T5 — Checklist 7/7
1/7 verde hoje (backup <24h). TRAVADA, Sentinela amarelo, 4 críticos unlock,
CMV 31,4%, rank 0d, QA sem runs — esperado.

---

## 2026-08-10 (20ª parte) — Pós-merge hotfix (H1 CMV CSP + H2 push bloqueado + H3 backups)

### H1 — CMV "Carregando…" permanente
Causa raiz: `<script>` inline em `app/Views/dashboard/cogs.php` **sem**
`nonce="<?= CSP_NONCE ?>"`. CSP (`script-src 'nonce-…' 'strict-dynamic'`)
bloqueava o JS → contadores "—" e tabela travada. API `/api/cogs/audit` estava OK.
Correção: nonce + listeners (sem onclick). Evidência browser: total=35,
real=11, missing=24, 35 linhas/badges. Salvar MLB4435358255 unit_cost=12.34
refletiu em `sku_custos.custo_produto=12.3400` (escrita local, zero ML).

### H2 — Push origin/master
Host key GitHub resolvido em `~/.ssh/known_hosts` (fingerprints oficiais
RSA/ECDSA/ED25519). Porém **não há chave privada SSH** no usuário eskill
(`Permission denied (publickey)`). `gh` sem login. PARADO — precisa de deploy
key / SSH key autorizada no repo `jessestainx/eskill` ou remote HTTPS com
credencial. Não inventar remote.

### H3 — Rotação de backups
Antes: 45G em storage/backups, disco 73%. Script `bin/backup-rotate.sh`
(política: 3 .sql íntegros recentes + marcos de onda em .gz). Cron semanal
domingo 04:30. Inventário e df no relatório.

`ML_WRITE_AUTOMATION=false` intacto. Branches mergeadas NÃO deletadas.

---

## 2026-08-10 (19ª parte) — MERGE ÚNICO em master (Ondas 2 → 3 → 3.1 → 2.1 → 3.5)

### Autorização
Usuário (verificador independente) autorizou merge após verificação em produção
das Ondas 2, 3, 3.1, 2.1 e 3.5. `ML_WRITE_AUTOMATION=false` intacto.

### Inventário pré-merge
- Working tree limpo (só untracked de ferramentas: `.claude/`, `.codacy/generated/`).
- Stash vazio.
- Branches tip: fix/onda2-consistencia `14a8c7d6`, fix/onda3-operacional `062ce886`,
  fix/onda3-1-agentes `3eb68761`, fix/onda2-1-pendencias `d9fb7a17`,
  feat/onda3-5-dados `791c7ce1`.
- `git pull` SSH falhou (Host key verification) — master local já estava
  alinhado a `origin/master` em `009e1389`.

### Backup MySQL
- Retry OK: `storage/backups/pre-merge-master-20260810_030204.sql` (~5.49GB,
  "Dump completed"). Tentativa anterior incompleta (Error 1412) descartada.

### Merges (ordem cronológica) — todos fast-forward, ZERO conflitos
1. master `009e1389` → fix/onda2-consistencia `14a8c7d6`
2. → fix/onda3-operacional `062ce886`
3. → fix/onda3-1-agentes `3eb68761`
4. → fix/onda2-1-pendencias `d9fb7a17`
5. → feat/onda3-5-dados `791c7ce1` (= tip final)

### Pós-merge
- PHPUnit: **4434 testes / 0 falhas** (skipped 27). Baseline 4429 + 5 da 3.5.
- `bin/deploy.sh`: adicionado restart de workers long-lived
  (`agent-runtime-monitor`, `pregao-tick`, `pregao-ws`) + reload PHP-FPM
  (lição Onda 3.1). Flag `--workers`.
- Branches mergeadas NÃO deletadas (aguardam OK do smoke independente do usuário).

### Hotfix pós-smoke
- `AccountXRayController::unlockPlan/resolveUnlockItem` usavam `$this->request`
  (controller não estende BaseController) → HTTP 500. Corrigido para `$_GET`
  + `activeAccountId()`, padrão dos demais endpoints do mesmo controller.
- `deploy.sh`: fallback SIGTERM no MainPID quando `systemctl restart` sem
  permissão (User=eskill + Restart=always).

### Hash final master
ver `git rev-parse HEAD` após commits desta sessão (`acdfd894`+hotfix).

---

## 2026-08-10 (18ª parte) — Onda 3.5: Dados e Negócio (CMV, Destravamento, Watchlist, Multi-conta, Narrativa)

### Contexto
Onda de dados/negócio (não bugs). Conta FACILYTY TRAVADA (score 34), Sentinela
verde operacional, loop de otimização bloqueado até CMV confiável + conta
destravada. Branch `feat/onda3-5-dados` a partir de `fix/onda2-1-pendencias`.
Backup: `storage/backups/pre-onda3.5-20260810_021625.sql` (5.49GB).
`ML_WRITE_AUTOMATION=false` intacto — planos manuais apenas.

### T1 — CMV
- Auditoria real: 35 anúncios com venda/90d; 10 com CMV real (`sku_custos`);
  25 faltando (lista no `/dashboard/cogs`).
- Novo `CogsService` + `CogsController` + view `/dashboard/cogs` (edição inline +
  CSV MLB,custo) + histórico `item_cogs_history`.
- Fonte canônica do P&L continua `sku_custos`; badge "real/estimado" no P&L
  (`financials.php`) + link para cadastro.
- `ItemService::updateItemCost` agora espelha em `sku_custos`.

### T2 — Plano de Destravamento
- Novo `AccountUnlockPlanService` + tabela `account_unlock_plan_items`.
- Aba no Raio X; reconstrói a partir do governance + blockers + moderação API.
- MLB7346643828: `under_review` + `sub_status=forbidden` + catálogo MLB75435443
  (motivo real da API, não genérico).
- 3 bloqueios de catálogo ativos no plano (impact 40, CRITICA).
- Mark resolved + auto-reconcile no próximo Raio X (só governance).

### T3 — Watchlist
- Infra já existia (`competitor_items` + `pregao-watchlist.timer`) mas vazia.
- Seed via highlights/categoria (search API do app está `forbidden`).
- 9 concorrentes inseridos; Pregão `Concorrentes available=true count=9`.
- APIs: `/api/pregao/watchlist` CRUD + seed + collect.

### T4 — Segunda conta AWA
- Fluxo OAuth já pronto em `/dashboard/accounts` → `/auth/authorize`.
- Checklist: `docs/ops/AWA_MOTOS_CONNECT.md` (usuário conecta pessoalmente).
- Fix colateral: `config/app.php` key agora lê `$_SERVER`/`getenv` além de
  `$_ENV` (GPCS sem E quebrava decrypt no CLI → `account_disconnected`).

### T5 — Narrativa VERDE vs TRAVADA
- Escolha: **duas dimensões explícitas** (não colapsar no pior).
- Pregão: `Operação: VERDE · Catálogo/Governança: TRAVADA` + tooltip.
- Snapshot inclui `governance` do último Raio X (score 34 / TRAVADA).

### Validação
PHPUnit Unit: **4434 testes / 0 falhas** (+5 novos). Zero escrita ML.

---

## 2026-08-10 (17ª parte) — Onda 2.1: Parte A (Perguntas, bulk selection, heading duplicado)


### Contexto
Usuário trouxe prompt "Onda 2.1" com 3 pendências supostamente nunca executadas
(A1 Perguntas, A2 updateBulkSelection duplicado, A3 heading duplicado em
Promoções) + Parte B (merge das branches fix/onda2-consistencia →
fix/onda3-operacional → fix/onda3-1-agentes em master, autorizado mas condicionado
à verificação da Parte A pelo usuário antes de executar). Branch
`fix/onda2-1-pendencias`, criada a partir de `fix/onda3-1-agentes`. Backup MySQL
completo antes de começar (`storage/backups/pre-onda2.1-20260810_015754.sql`,
5.49GB, sem erros).

### Descoberta: A2 e A3 já estavam corrigidos (sem ação necessária)
Investigação encontrou que A2 (commit `d5986a0f`, "unifica updateBulkSelection()
e funções irmãs duplicadas") e A3 (commit `8f569a4c`, "remove bloco duplicado
inteiro em /dashboard/promotions") já foram corrigidos na Onda 2, e esses commits
já estavam na branch `fix/onda3-1-agentes` de onde parti. Verificado ao vivo
(sessão admin real via curl, conta 1335): `/dashboard/items` carrega
`items-dashboard.js` uma única vez com uma única definição de
`updateBulkSelection()`; `/dashboard/marketing/promotions` renderiza exatamente
1x "Central de Promoções" (título de página) e 1x "Promoções Disponíveis"
(header do card) — textos diferentes, nunca foram duplicados um do outro.
Nenhuma mudança de código necessária para A2/A3.

### A1 — Perguntas: causa raiz NÃO era mais fonte de dados divergente
Commit `910a7545` (Onda 2) já unificou tabela e contadores em
`QuestionService::getQuestionsLocal()` — confirmado ao vivo:
`/api/questions/stats` retorna `total:45, answered:45, pending:0`, e
`/api/questions?limit=10` retorna as 45 perguntas reais com `source:"local"`.
A causa raiz ATUAL (reproduzida ao vivo) é outra: o filtro padrão da tela em
`app/Views/dashboard/questions.php` (`#questionFilter`, opção `unanswered`
marcada `selected`) faz `loadQuestions()` chamar
`/api/questions?status=unanswered&...` no carregamento inicial — para uma conta
com 0 perguntas pendentes (45/45 já respondidas), a API retorna corretamente
`questions: []` para esse filtro. A mensagem genérica "Nenhuma pergunta
encontrada" ao lado de "Total: 45" no topo da tela parecia inconsistência de
dados, mas era o resultado esperado do filtro ativo.
**Correção**: mensagem de estado vazio agora varia por filtro selecionado
(unanswered → "Nenhuma pergunta pendente — todas já foram respondidas! 🎉";
answered → "Nenhuma pergunta respondida ainda"; all → "Nenhuma pergunta
encontrada para esta conta"). Verificado ao vivo: página `/dashboard/questions`
já serve o JS atualizado (sem restart necessário — views PHP não sofrem o
problema de bytecode preso de workers systemd de longa duração da Onda 3.1).

### Validação
Suite Unit completa rodada ao final: **4429 testes, 10488 assertions, 0
falhas**. `ML_WRITE_AUTOMATION=false` intacto; todas as verificações desta
sessão foram leituras (sessão admin de diagnóstico criada diretamente no Redis
para inspeção read-only via curl, destruída ao final; nenhuma escrita no
Mercado Livre).

### Pendente (aguardando autorização explícita do usuário)
Parte B (merge sequencial fix/onda2-consistencia → fix/onda3-operacional →
fix/onda3-1-agentes → fix/onda2-1-pendencias em master + deploy + restart de
workers + smoke test) **não foi executada** nesta sessão — usuário pediu para
aguardar a verificação da Parte A em produção antes de autorizar.

---

## 2026-08-09/10 (16ª parte) — Onda 3.1: FINANCEIRO/ORQUESTRADOR no cron real + Frete + Raio X + Diagnóstico + QA Playwright

### Contexto
Verificação independente em produção (sessão admin real) confirmou que FINANCEIRO/
ORQUESTRADOR continuavam FALHOU mesmo após a Onda 3, além de 4 bugs novos: Frete
("Erro ao carregar pedidos"), Raio X (account_status não persistido + card
desatualizado), Diagnóstico ("score atual: undefined") e QA Playwright ("erros JS
e HTTP"). Branch `fix/onda3-1-agentes`, a partir do estado deployado (não master).
Baseline PHPUnit 4429/0 falhas mantida do início ao fim (rodada após cada bloco).

### F1/F2 — FINANCEIRO/ORQUESTRADOR: causa raiz era um worker com bytecode velho
O fix de schema da Onda 3 (`5157525f`) estava correto no arquivo, mas o processo
`agent-runtime-monitor.service` (PID 3218) era de longa duração e nunca recarregou
o código — rodava a versão pré-fix do allowlist indefinidamente. `SIGTERM` no PID
forçou o systemd a reiniciar o worker com o código atual; ciclo seguinte confirmou
FINANCEIRO e ORQUESTRADOR em `success` no `pregao_events`/journalctl.
Adicionado, para o caso de recorrência: `AgentRuntimeFactory::logReadFailure()`
agora inclui `exception_class/message/file:line/trace` resumido;
`logFinanceiroValidationFailure()` loga exatamente qual sub-check de schema
rejeitou o snapshot fail-closed quando NENHUMA exceção é lançada (schema drift
silencioso — antes "financeiro unavailable" não tinha causa raiz rastreável);
`AgentRuntimeWorker::logAggregateFailure()` lista, quando o ORQUESTRADOR falha,
quais sub-agentes falharam e com que `reason()` (antes só "agent failed" genérico).

### F3 — /dashboard/shipping "Erro ao carregar pedidos" (commit ba0992cb)
Causa raiz: `OrderService::listOrders()` mandava o filtro `status` da UI direto
para `order.status` na busca da API do ML. Status de logística (`ready_to_ship`,
`shipped`, `delivered`, `handling`, `not_delivered`) não são aceitos nesse
parâmetro — API responde HTTP 400 "Invalid filters", traduzido em "Erro ao
carregar pedidos" pela tela. Adiciona `ML_ORDER_STATUSES`/`ML_SHIPPING_STATUSES` e
roteia cada filtro para `order.status` ou `shipping.status` conforme o tipo; expõe
`shipping_status` no resumo do pedido; ajusta fallback local (cache) e frontend
(com defensivos para data/status ausentes).

### F4 — Raio X: account_status não persistia + card desatualizado (commit d5f828aa)
Causa raiz 1: `AccountXRayService` lia `$govResult['classification']['account_status']`,
mas `AccountGovernanceService::runFullDiagnostic()` retorna `account_status` na
RAIZ do array — caía sempre em UNKNOWN/NULL mesmo com o diagnóstico resolvendo o
status real. Causa raiz 2: query do card/histórico ordenava só por `created_at
DESC`, sujeita a empate — adicionado `id DESC` como desempate em
`AccountXRayService::listReports` e no subquery de `AccountXRayController`.
Verificado com scan real: account_status persistido, card mostra o scan mais novo.

### F5 — Diagnóstico "score atual: undefined" (commit 7d2bc3be)
`AccountHealthService::generateWeeklyPlan()` retorna `focus_pillar.score`, JS lia
`focus_pillar.current_score` (campo inexistente). Corrigido mapeamento +
fallback numérico defensivo em `estimated_gain`.

### F6 — QA Playwright "erros JS e HTTP" (commit 2a7759a1) — causa raiz real, não "by design"
Execução manual pelo MESMO caminho do cron (mesmo `bin/pregao-qa-worker.php`,
mesmo usuário `eskill`, mesmo ambiente do `pregao-qa-worker.timer`) reproduziu
2 bugs reais:
1. `bin/pregao-qa-browser.mjs`: allowlist de rede read-only nunca foi atualizada
   quando FontAwesome (`@fortawesome/fontawesome-free@6.5.1`, usado em 12 views)
   foi adicionado a `layouts/modern/app.php` — CSS classificado como "egress
   externo inesperado", bloqueando a etapa `dashboard` inteira.
2. `app/Security/PregaoQaReadOnlyGuard`: allowlist tinha `/api/dashboard/notifications`
   mas o sino de notificações real (`public/js/api-client.js`) chama
   `/api/notifications?limit=5` — rota ausente, respondendo 403 "Sessão QA
   somente leitura bloqueada" em toda carga do dashboard, contado como erro HTTP.
Corrigidas as duas allowlists. Trocado o booleano `stderr_present` (sem conteúdo)
por `stderr_excerpt` (até 4KB) propagado ao log de falha; o runner mjs agora
escreve no stderr o motivo exato de cada console.error/pageerror/HTTP>=400.
Evidência: run real enfileirado via `PregaoController::qaRun()` (mesmo endpoint
do botão "QA Playwright: Executar"), processado pelo `pregao-qa-worker.timer`
real (não é chamada isolada) — `run_id=2632b6a4-3836-4181-ac2d-05bc6bf655d9`
completou com `status=passed` nas 5 etapas, confirmado lendo o estado persistido
(Redis + `pregao_events`, `receipt_event_id=354540`).

**Descoberta sobre `QaMergeGate`/`QaAgent` (decisão do dono, não implementada)**:
investigação revelou que esse gate baseado em `QA_GATE_*` (env vars só-do-CI) e o
`QaAgent` **nunca rodam no ciclo de cron de produção** — `AgentRuntimeExecutor::
execute()` rejeita explicitamente qualquer modo != `'monitor'`
(`InvalidArgumentException`), e `createRoster('monitor')` não inclui `QaAgent`.
Além disso a grade "AGENTES 24/7" do Pregão (`dashboard/pregao.php`) só tem 5
tiles (SENTINELA/COLETOR/FINANCEIRO/OTIMIZADOR/ORQUESTRADOR) — não existe card
"QA" ali. Ou seja: `QaMergeGate`/`QaAgent` são código inerte em produção, sem
nenhum efeito visível, só acionável manualmente via CLI (`bin/agent-runtime.php
--mode=qa/all`) ou usados pelo `bin/qa-merge-gate.php` dentro do próprio job de
CI. Perguntado ao dono se deveria (a) deixar como está, (b) ligar a um card novo
+ scheduler automático (capability nova de verdade), ou (c) remover. Decisão:
(a) deixar como está — documentado aqui, nenhuma mudança de comportamento visível
a ganhar, e `QaAgent.php`/`QaMergeGate.php` têm teste de pureza deliberado
(`testImplementacaoNaoPossuiCapacidadesDeShellOuIntegracoesExternas`, barra
menção a PDO/Database/git/deploy no código-fonte) que seria violado sem motivo.

### Validação final
Suite Unit completa rodada ao final: **4429 testes, 10488 assertions, 0 falhas**.
`ML_WRITE_AUTOMATION` permanece `false`; nenhuma escrita no Mercado Livre em
nenhum momento desta sessão (todas as verificações via `bin/agent-runtime.php`
read-only, sessões QA read-only via `PregaoQaSessionService`, e leituras diretas
de `pregao_events`/Redis). Nenhum segredo em diff/commit/log.

### Pendente / não implementado nesta sessão
- Merge de `fix/onda3-1-agentes` em `master` — decisão do usuário, não feita.
- Persistência de evidência de CI (`QA_GATE_*`) para runtime — decisão tomada:
  não fazer, pois não há pipeline de deploy remoto real (deploy.yml roda em
  checkout efêmero do GitHub, desconectado deste servidor) e não há card afetado.

---

## 2026-08-09 (15ª parte) — Onda 3: cruzamento do relatório de investigação com o código real

### Contexto
Usuário trouxe um relatório de investigação profunda de outra sessão/agente sobre
eskill.com.br (produção), listando ~20 achados (token OAuth expirado, claims sem
filtro, returns 500, timezones divergentes, itens duplicados em JS, promoções 404,
monitoramento zerado, Full Restock NaN, agentes FINANCEIRO/ORQUESTRADOR falhando,
CMV faltante, veto do Sentinela, QA interno falhando).

### Descoberta: Onda 1 e Onda 2 já estavam corrigidas
A pasta de produção estava no branch `fix/onda2-consistencia` (não master) com
commits de hoje (16h36–19h37 UTC) que já cobriam token/claims/returns/timezone
(`2ee23662`), duplicações JS em itens/perguntas (`0ec92bef`, `d5986a0f`,
`910a7545`), reconciliação Dashboard/Analytics/Vendas (`645b3aee`, T1–T9) e o link
de Promoções (`8f569a4c`). Confirmado ao vivo: token ativo até 10/08 03:29, rota
`/dashboard/marketing/promotions` correta. Branch nunca foi mergeada em master nem
documentada no claude-progress.txt por quem fez — risco de auditoria registrado.

### Onda 3 — trabalho desta sessão (branch fix/onda3-operacional)

**fix(agents) `5157525f`** — FINANCEIRO/ORQUESTRADOR do Pregão mostravam FALHOU.
Causa raiz: `PnlReportService::getPnL()/getMetrics()` ganharam campos novos
(`cogs_source`, `advertising_expenses`, `units_sold`, `source`, `cash`) em mudança
anterior, mas o allowlist estrito fail-closed de `AgentRuntimeFactory`/
`FinanceiroAgent` (que valida snapshot antes de expor no card) não foi atualizado
— toda leitura caía em `invalid_legacy_payload`/`financeiro_unavailable`, e
ORQUESTRADOR falha em cascata se qualquer sub-agente falha. Atualizado schema nos
dois arquivos (PNL_KEYS, METRICS_KEYS, CASH_KEYS, validação e normalização).
Verificado via `bin/agent-runtime.php --account-id=1335 --mode=all` (read-only):
financeiro passou de `failed/financeiro_unavailable` para
`success/legacy_read_complete`.

**fix(monitoring) `dcd977da`** — `/dashboard/monitoring` mostrava CPU/Memória/
Disco/DB/Cache sempre 0%. Causa raiz: contrato quebrado entre o JS da view (lê
`data.cpu`/`data.memory`/`data.disk`/`data.db.*`/`data.cache.*` no nível raiz da
resposta) e `MonitoringController::metrics()` (retornava
`{success, metrics: {cache, queue, llm, database, system}}` — nada batia).
Reescrito para montar o payload exatamente na forma esperada, reaproveitando
`AdvancedMonitoringService::collectSystemMetrics()` (já real: sys_getloadavg,
disk_total_space/disk_free_space, SHOW GLOBAL STATUS) e
`PerformanceMetricsService::getMetrics()` (Redis real). Adicionadas queries reais
(não mockadas) para requests_by_hour/response_times (`performance_logs`) e
recent_errors (`system_alerts`) — hoje retornam vazio porque nada grava nessas
tabelas ainda (gap separado: o middleware `logRequestPerformance()` existe mas não
está registrado no pipeline de requisições — não corrigido nesta sessão, fora de
escopo/risco maior). Verificado via reflection (sem HTTP, read-only): CPU 36.66%,
Disco 61.66%, DB 3 conexões, cache hit_rate 66.3% — dados reais, não mais zerados.

**Full Restock NaN**: já corrigido na Onda 2 (comentário no código referencia
"Onda 2 / T5"); `days_coverage` retorna `null` (não NaN/999) quando não há vendas
30d. Confirmado ao vivo via reflection sobre `FullController::
calculateRestockSuggestions()` para a conta 1335 — valores corretos.

**Veto do Sentinela e "15 anúncios sem CMV"**: confirmados REAIS e corretos.
`Sentinela::getDashboard(1335)` retorna exatamente o que o relatório descreveu —
MLB7346643828 em `under_review` + 3 itens de catálogo `inactive` (2,52%, dentro do
limite 5%, semáforo amarelo). Isso é a governança funcionando como desenhado, não
um bug — exige ação manual do dono (contestar moderação, corrigir/mesclar catálogo
duplicado). O alerta "N anúncios sem CMV" com link "Cadastrar custos" já existe em
`orders-content.php`; "enforcement" (bloquear algo) seria capability nova, não
implementado sem prompt escopado.

**Card QA do Pregão sempre falha (`invalid_qa_results_snapshot` /
`qa_merge_gate_rejected`)**: por design atual, não é bug de lógica.
`QaMergeGate::assertPasses()` só passa se 4 env vars `QA_GATE_*` estiverem
`'passed'` — mas essas env vars só existem transientemente dentro do job do
GitHub Actions (`.github/workflows/deploy.yml`, step que roda
`bin/qa-merge-gate.php`), nunca no processo PHP-FPM real de produção. Não há
persistência de evidência de CI para leitura em runtime. Corrigir isso de verdade
exige uma decisão de arquitetura (como/onde persistir "commit X passou CI aos
Y") — não implementado, é uma decisão do dono, não um fix mecânico.

**Achado de segurança/infra à parte**: ~199 arquivos de `app/Controllers` e
`app/Services` estavam com dono `root:root` (só leitura para o usuário `eskill`),
bloqueando edição direta (provavelmente resíduo de deploy anterior rodado como
root). Corrigido rodando `clpctl system:permissions:reset` (sudo NOPASSWD
liberado para esse wrapper) — os 340 arquivos passaram a `eskill:eskill`. Comando
demorou ~7min e imprimiu vários "chmod: invalid mode" no meio (provavelmente
symlinks quebrados em `.worktrees`), mas terminou e resolveu o problema.

### Validação
Suite Unit completa: **4429 testes, 10488 assertions, 0 falhas** (rodada 2x,
antes e depois do conjunto completo de mudanças). Nenhuma escrita no Mercado
Livre em nenhum momento — todo diagnóstico foi via `bin/agent-runtime.php`
(read-only) e reflection sobre métodos privados de leitura.

### Pendente / não implementado nesta sessão (fora de escopo de housekeeping)
- Middleware de `performance_logs` não registrado no pipeline (requests_by_hour/
  response_times ficam vazios até isso ser ligado).
- Persistência de evidência de CI para o QaMergeGate funcionar em runtime.
- Merge de `fix/onda2-consistencia`/`fix/onda3-operacional` em `master` (housekeeping
  de git pendente, não fiz porque não foi pedido explicitamente).

---

## 2026-08-09 (14ª parte) — ALERTA: OAuth staging conectou a conta real FACILYTY (1335) por engano

- OAuth com a app dedicada (`4716547571285773`) funcionou tecnicamente: `OAuth callback success` às 20:16:12, criou `ml_accounts.id=1337` no staging.
- **PROBLEMA CRÍTICO**: `ml_user_id=3058804121` / nickname `FACILYTY` é a MESMA conta real do Mercado Livre que já está cadastrada em produção como `ml_accounts.id=1335` (confirmado por query cruzada). ID interno diferente (1337 vs 1335) mas é o mesmo vendedor real — qualquer escrita de preço "no staging" para essa conta afetaria anúncios reais em produção.
- Isso violou a instrução explícita do prompt aprovado e do STAGING.md ("conectar conta ≠1335") — o usuário logou no OAuth com a conta principal errada (a de produção, não uma conta de teste separada).
- **Ação de contenção imediata**: `ml_accounts.status` da conta 1337 setado para `inactive` no staging; `users.active_ml_account_id` limpo se apontava para ela. Confirmado: `PREGAO_ACCOUNT_ID` continuava `0` (guard do tick já bloquearia); zero itens com `auto_reprice=1` associados a 1335/1337 no staging; nenhuma escrita de preço ocorreu.
- **Pendente**: dono precisa conectar uma conta de teste DIFERENTE da FACILYTY/1335 (outro CPF/CNPJ vendedor, não colaborador da mesma conta). Repeti a explicação de "conta principal" mas o usuário aparentemente só tem acesso à conta de produção real.
- F1 do repricing continua bloqueada — não por infra agora, mas por falta de uma conta ML de teste genuinamente separada da produção.

---

## 2026-08-09 (13ª parte) — Staging passou a usar app ML dedicada

- `.env` staging: `ML_APP_ID`/`ML_CLIENT_ID` = `4716547571285773` (app dedicada), `ML_REDIRECT_URI=https://staging.eskill.com.br/auth/callback`.
- Produção permanece na app antiga `1643774977535598` (não alterada).
- `getOAuthConfigDiagnostics()` staging → ready=true.
- Fixture `1336` segue inactive; aguarda OAuth real com a app nova (conta principal, redirect cadastrado nessa app).

---

## 2026-08-09 (12ª parte) — Staging DNS+HTTPS no ar; OAuth ML ainda pendente

- DNS público: `staging.eskill.com.br` → `72.62.14.91` (Registro.br NS).
- TLS: certbot emitiu LE para `staging.eskill.com.br` (válido até 2026-11-07); `https://staging.eskill.com.br/api/health` → 200.
- Isolamento: `deploy_staging.sh --check` OK.
- Bloqueio restante Fase 1: OAuth ML conta ≠1335 + `PREGAO_ACCOUNT_ID` (hoje `0`; só fixture `1336`). `pregao-tick-staging` continua disabled.
- ml-health-check ainda falha probes de reachability ML (HTTP 403 em auth/api externos) + `/users/me` no fixture; callback HTTPS staging responde 302.

---

## 2026-08-09 (11ª parte) — Repricing F1 staging: pipeline pronto, bloqueio humano OAuth/DNS

### Objetivo
Destravar Fase 1 do repricing automático (prompt aprovado `.github/prompts/repricing-automatico.prompt.md`) sem tocar produção 1335.

### Feito
- `bash scripts/deploy_staging.sh --check`: isolamento OK (Redis DB1, MySQL `eskill_staging`, health Host=staging→200).
- Permissões `storage/locks` + `storage/reports` staging corrigidas via `clpctl system:permissions:reset` (estavam root:root e bloqueavam simulação).
- `php bin/reprice-simulate.php --account=1336 --limit=5`: 5 candidatos, 0 apply, todos `skip: market_min_invalido` (fixtures sem mercado real).
- 5 itens já opt-in: `MLBSTG00000001`–`05` com `auto_reprice=1` (demais fixtures 0).
- Staging `ai_agents`: registro `sniper` status=`active` (id=4).
- Enfileirados `run_agent` jobs `#114696` e `#114697` (account_id=1336); processados com `php bin/jobs-once-worker.php --type=run_agent` **no path staging** → `completed` / `success=true`.
- Preços locais inalterados após 2 runs (sem dados de mercado + token fixture fake) — confirma que não houve escrita fantasma.
- E2E staging (`PLAYWRIGHT_BASE_URL=http://staging.eskill.com.br`): **96 passed / 16 skipped / 0 failed** (Chromium instalado via `npx playwright install chromium`).

### Bloqueio humano (F1 end-to-end de escrita ML ainda NÃO verde)
1. DNS público: `staging.eskill.com.br` A → `72.62.14.91` (hoje só `/etc/hosts`→127.0.0.1; dig público vazio).
2. CloudPanel/TLS: emitir cert e habilitar 443 (`config/nginx/staging.eskill.com.br.conf`).
3. OAuth ML: cadastrar `https://staging.eskill.com.br/auth/callback`, conectar conta **≠1335** (substituir fixture `1336` / `STAGING_UI_FIXTURE`).
4. `.env` staging: `PREGAO_ACCOUNT_ID=<id real>`; nunca 1335.
5. `systemctl enable --now pregao-tick-staging` (root).
6. Observação operacional: cron `jobs-once-worker` do user `eskill` aponta só para **prod** — jobs staging precisam de one-shot no path staging (ou cron staging separado no futuro; fora deste escopo).

### Status
- `project-status.json` → `repricing-automatico.passes` permanece `false` (escrita ML staging ainda bloqueada por OAuth/DNS).
- Produção 1335: zero escrita; `SAFE_MODE=true`, `FORBIDDEN_ACCOUNTS=1335`, `ML_WRITE_AUTOMATION=false`.

---

## 2026-08-09 (10ª parte) — SEO Killer: descrição, engajamento e pesos rebalanceados

### SEOScoreCalculator.php — 4 melhorias adicionais

**`scoreDescriptionQuality` (refatoração completa):**
- REMOVIDO: `keyword_density` como critério — incentivava keyword stuffing na descrição; documentado no código o motivo da remoção
- Adicionado: checks de conteúdo útil (compatibilidade, instalação, garantia) via `$utilityKeywords`
- Estrutura: check melhorado (parágrafos, itens de lista, quebras de linha)
- Extensão: thresholds mais precisos (0=−50, <150=−35, <300=−15, >3000=−5)

**`scoreEngagement` (novo: taxa de conversão):**
- Antes: score baseado apenas em `sold_quantity` com thresholds fixos
- Agora: combina histórico de vendas + taxa de conversão (visitas→vendas)
- Anúncio com visitas mas zero vendas → score 35 + issue diagnóstico
- Conversão < 1% → penalidade + issue orientado ("revise preço, imagens e título")
- Anúncios novos (< 10 visitas) → score neutro 50 (sem penalizar por falta de histórico)

**`generateRecommendations` (ordenação por impacto real):**
- Antes: ordenava por `priority === 'high'` (binário, arbitrary)
- Agora: ordena por `weight × (100 − componentScore)` — recomendação mais impactante no score final aparece primeiro

**`WEIGHTS` rebalanceados:**
- `attributes: 15% → 20%`: atributos habilitam filtros (Category Fit) — maior impacto na visibilidade orgânica do ML
- `description: 20% → 15%`: descrição é importante para conversão mas secundária para indexação no ML

### Resultado
- 214 testes, 473 assertivas: todos passando
- php -l: OK, ReadLints: sem erros

---

## 2026-08-09 (9ª parte) — SEO Killer: scoring corrigido com melhores práticas ML

### SEOScoreCalculator.php — 3 métodos corrigidos

**`scoreTitleQuality` (antes: threshold errado + regex problemática):**
- Comprimento: range corrigido para 45-80 chars (era "50-60" penalizando > 70 — o ML suporta até 80 chars visíveis)
- Símbolos proibidos: nova checagem de `%, #, !, $, @, &, *` etc. (-15 pts)
- Caps lock: melhorado (era `/[A-Z]{5,}/`, agora verifica todo o título vs mb_strtoupper)
- Keyword stuffing: regex `/(\b\w+\b).*\1/i` substituída por contagem de frequência — palavras de 4+ chars que aparecem 3+ vezes. A regex antiga flagueava "CG 160 Motor 160" como stuffing (falso positivo).
- Title Architecture: verifica BRAND e MODEL nos atributos do item (antes só verificava se existiam nos atributos, sem checar no título)

**`scoreAttributesCompleteness` (antes: contagem pura sem schema ML):**
- Substituída por verificação real via `getCategoryAttributes()` da API ML
- Distingue: `required` (penalidade até -50), `filterable`/`catalog_required` (até -25), `recommended` (até -15)
- Fallback para contagem simples quando a categoria não está disponível
- Retorna campos extras: `required_missing`, `filter_missing`, `recommended_missing`

**`scoreImagesQuality` (antes: só contagem):**
- Verifica resolução via campo `max_size`/`size` da API ML (formato "LARGURAxALTURA")
- Detecta imagens de alta resolução por sufixo `-O.` ou `-F.` na URL do CDN mlstatic.com
- Penalidade diferenciada: todas baixa res → -25; algumas baixa res → -5×n (máx -15)
- Retorna campos extras: `high_res`, `low_res`

### TitleKiller.php — `calculateTitleScore` corrigido
- Threshold de comprimento: 60 → 80 chars (era muito restritivo para peças de moto com compatibilidade)
- Palavra-contagem: limite > 10 (antes) → > 15 (agora), compatível com títulos de peças
- Power words removidas do scoring: "ORIGINAL", "GRÁTIS" etc. não contribuem para relevância semântica no ML
- Nova checagem de símbolos proibidos (-20 pts)

## 2026-08-09 (8ª parte) — Debug mode: spinner flash (x-cloak) + confirmação via nginx logs

- **Root cause definitivo do "spinner travado"** na Governança da Conta: era flash pré-Alpine.js (não hang real). Elementos `x-show="loading"` e `x-show="error"` ficavam visíveis antes do Alpine inicializar porque não havia `x-cloak`. Isso criava a ilusão visual de "spinner travado" mesmo sem nenhuma requisição em andamento.
- **Evidência nginx**: 5 chamadas POST ao `diagnostic-ml` às 05:24–05:30 UTC retornando HTTP 200 (~16s cada). Nenhuma das "reproduções" de debug gerou POST — o usuário só carregava a página e via o flash.
- **Fix** (`app/Views/dashboard/account-governance.php`): CSS `[x-cloak] { display: none !important; }` + atributo `x-cloak` nos divs `x-show="loading"` e `x-show="error"`. Página agora serve 12150 bytes e carrega limpa.
- **Toda instrumentação de debug removida**: `AccountGovernanceController.php`, `AccountGovernanceIntegrationService.php`, `account-governance.php` (JS fetch logs) — zero ocorrências de `debug-659e23` restantes.
- H-A (FPM timeout 7200s), H-B (Guzzle não paralelo), H-C (tempo excede timeout), H-D (PHP 500) — todas REJEITADAS por runtime evidence. H-E (flash pré-Alpine) — CONFIRMADA.

## 2026-08-09 (7ª parte) — Performance: getMultiItemVisits paralelo (Guzzle async) — de ~60s para ~8s em 183 itens

- **Root cause do timeout** (`/dashboard/account-governance` → "Processando diagnóstico..."): `getMultiItemVisits` fazia chamadas sequenciais de 1 item por vez com 30ms de sleep entre cada, totalizando ~60s para 183 itens — suficiente para estourar o timeout do PHP-FPM.
- **Fix — `MercadoLivreClient::fetchVisitAsync()` + `getMultiItemVisits()` (app/Services/MercadoLivreClient.php)**:
  - Adicionado método `protected fetchVisitAsync(string $id, int $days)` que retorna um `GuzzleHttp\Promise\PromiseInterface`. Encapsula URL + CF proxy + `httpClient->getAsync()`. `protected` para permitir override em testes sem mockar o httpClient do Guzzle.
  - `getMultiItemVisits` agora usa `array_chunk($itemIds, 10)` e `GuzzleHttp\Promise\Utils::settle()` para enviar 10 requisições paralelas por batch, aguardando todas antes de iniciar o próximo batch. Delay reduzido de 30ms/item para 50ms/batch.
  - Estimativa de melhoria: ceil(183/10) = 19 batches × ~450ms ≈ **8s** (antes: 183 × ~370ms ≈ 69s).
- **Fix — `AccountGovernanceController::runDiagnosticFromML()` (app/Controllers/AccountGovernanceController.php)**: Adicionado `@set_time_limit(120)` para dar margem de segurança a contas grandes além do ganho de performance.
- **Testes atualizados**:
  - `MLVisitsContractTest.php`: 2 testes migrados de `mock get()` para `mock fetchVisitAsync()` retornando `GuzzleHttp\Psr7\Response` via `Create::promiseFor()`. 7/7 passando.
  - `AdvancedSEOMaximizerTest.php`: `testScoreImagesSixOrMoreGivesMaxScore` atualizado para passar imagens com `size: '1200x900'` (metadado de qualidade real). 69/69 passando.
- **Suite completa**: 4429 testes, 10488 assertions, 0 falhas.

## 2026-08-09 (6ª parte) — 3 bugs corrigidos: HTML badges Account Health + AdvancedSEOMaximizer (3 bugs) + project-status.json (3 features falsamente passes:true)

- **Bug corrigido — HTML malformado nos badges de causas em Account Health** (`app/Views/dashboard/account-health.php` linha 3729):
  Template literal retornava `< span class = "badge...">` (espaços indevidos dentro das tags) em vez de `<span class="badge...">`. Os badges de causas (tipo_anuncio, sem_frete_gratis, saude_baixa, fora_catalogo) nunca apareciam na UI porque o navegador não interpretava as tags como HTML — eram renderizadas como texto literal. Corrigido para HTML válido.

- **Bug corrigido — `AdvancedSEOMaximizer::advancedCompetitorAnalysis()` item_id errado** (`app/Services/AI/SEO/AdvancedSEOMaximizer.php` linha 338):
  `'item_id' => $itemData` atribuía o array inteiro de dados do item ao campo `item_id` da análise, em vez do ID string `$itemId` passado como parâmetro. Corrigido para `'item_id' => $itemId`.

- **Bug corrigido — `AdvancedSEOMaximizer::advancedCompetitorAnalysis()` competitor item_id nulo** (linha 350):
  `$competitor['id']` retornava null porque `findDirectCompetitors` faz `SELECT ml_item_id, ...` — a coluna chama-se `ml_item_id`. Corrigido para `$competitor['ml_item_id'] ?? $competitor['id'] ?? null`.

- **Bug corrigido — `AdvancedSEOMaximizer::scoreImages()` sempre 100% para qualidade de imagem** (linhas 889-896):
  Loop incrementava `$highRes++` para toda imagem sem verificação alguma (comentário: "Simplificado: considerar todas como alta resolução"). Isso fazia o score de imagens sempre atingir o máximo independentemente da qualidade real. Corrigido: agora verifica o campo `size`/`max_size` da imagem ML (ex.: "1200x803") e conta como alta resolução apenas imagens com menor dimensão ≥ 800px; quando não há dado de tamanho, usa heurística de URL (URLs com "-O." indicam imagem original/alta resolução no CDN do ML).

- **project-status.json corrigido — 3 features marcadas falsamente como passes:true**:
  - `listing-irregularity-motor-readonly`: `ListingIrregularityScanService` e `ListingVisibilityController` não existem em nenhum arquivo do codebase. → `passes: false`
  - `sales-blocker-store`: `SalesBlockerStore`, `bin/sync-irregularities.php` e tabela `ml_sales_blockers` não existem. → `passes: false`
  - `listing-ui-store`: UI dependia de SalesBlockerStore (inexistente). → `passes: false`
  Agora há 4 features com `passes: false` (+ `repricing-automatico`). Nenhuma feature foi removida — apenas campo `passes` atualizado conforme regra de ouro.

## 2026-08-09 (5ª parte) — Diagnóstico "por que o anúncio não vende": mapeamento do que existe + 2 bugs corrigidos (Buy Box falso-positivo, Governance com métricas 14d zeradas)

- **Contexto**: dono perguntou se o sistema já detecta motivos de baixa venda por anúncio. Investigação (subagent `explore` + verificação manual) mapeou tudo que existe hoje: `AccountHealthService` (anúncios parados 60+ dias com 4 causas prováveis: tipo de anúncio, sem frete grátis, saúde ML baixa, fora do catálogo) e `AccountGovernanceService` (classificação TOXICO/POLUIDOR/MORTO/FRACO/EM_RISCO/SAUDAVEL/ANCHOR por tráfego+conversão+estoque). **Não existe** um motor único que combine, por anúncio, Buy Box + preço vs. concorrente + frete + perguntas/reclamações do item + reviews do produto + sazonalidade numa única explicação causal — isso ficou registrado como gap, não implementado (é capability nova, exige prompt escopado por política do projeto).
- **Achado colateral**: `project-status.json` marca `passes: true` para "listing-irregularity-motor-readonly" e "sales-blocker-store" (`ListingIrregularityScanService`, `SalesBlockerStore`, `bin/sync-irregularities.php`, `ml_sales_blockers`). As classes existem, mas só dentro de `app/Services/_runtime/` — um bootstrap isolado (`app/Services/_runtime/bootstrap.php`, autoload próprio via `spl_autoload_register`) usado **exclusivamente** pelos 4 scripts standalone `bin/awa-*.php` (Awa Motos), sem nenhuma rota/controller no app principal e sem `bin/sync-irregularities.php` (não existe na árvore). Ou seja, não está acessível a partir do dashboard/API que o usuário usa. Não corrigido nesta sessão (aguardando decisão do dono sobre marcar `passes: false` ou apenas documentar).
- **Bug 1 corrigido — Buy Box falso-positivo** (`/dashboard/catalog/competition`):
  - `ItemService::getCatalogDetails()` (`app/Services/ItemService.php`) sempre fabricava `buy_box_winner = {price: 0, seller_id: 0}` mesmo quando a API `/products/{id}` não retornava nenhum vencedor real (produto sem disputa ativa). Como o array sempre tinha chaves preenchidas, `empty($catalogData['buy_box_winner'])` em `CatalogController::listLosingItems()` nunca filtrava esse caso — o item aparecia na UI como "Perdendo" com preço-para-vencer R$0,00.
  - Fix: `getCatalogDetails()` agora retorna `buy_box_winner: null` + `status: 'no_active_competition'` quando não há winner real; `CatalogController` pula esses itens em vez de reportar perda fictícia.
  - Bug adicional na mesma tela: `app/Views/dashboard/catalog/competition.php::processData()` recalculava `is_winning` só comparando preço no frontend (`my_price <= buy_box_winner.price`), **ignorando** o campo `is_winner` real vindo do backend (que compara `seller_id`, o critério correto — preço não é o único fator da Buy Box). Fix: JS agora usa `item.is_winner` do servidor como fonte da verdade, só cai no cálculo por preço se o servidor genuinamente não informar o campo.
- **Bug 2 corrigido — Account Governance com métricas de 14d sempre zeradas** (`/dashboard/account-governance`):
  - `AccountGovernanceIntegrationService::formatItemForGovernance()`/`enrichItemsWithMetrics()` (`app/Services/MercadoLivre/AccountGovernanceIntegrationService.php`) só populavam `visits_30d`/`sales_30d`; `visits_14d`/`sales_14d` nunca eram preenchidos (ficavam sempre 0 pelo default do `AccountGovernanceService::calculateFlags()`).
  - Impacto real: com `visits_14d=0` fixo, `trend = (projected14d - visits30d) / visits30d = -1` (−100%) para **todo item com qualquer tráfego em 30d**, disparando a flag `FALLING` (limiar é −30%) indiscriminadamente — todo anúncio com tráfego era classificado como "tráfego em queda" mesmo quando estável ou crescendo. Da mesma forma, `sales_14d=0` fixo disparava `NO_SALES_14` mesmo em itens vendendo ativamente.
  - Fix: `MercadoLivreClient::getMultiItemVisits()` agora captura `visits_detail` (detalhamento diário) que a API já retorna mas era descartado (`'daily' => []` fixo); `AccountGovernanceIntegrationService` soma os últimos 14 dias a partir desse detalhamento (`sumLastNDaysFromDaily()`, sem chamada extra à API), com fallback proporcional (`visits_30d * 14/30`) só se a resposta vier em formato legado sem detalhamento diário. Para vendas, `fetchSalesByItem()` passou a computar `sales_14d` filtrando pela mesma lista de pedidos já buscada para os 30d (por `order['date_created']`), sem chamada adicional a `/orders/search`.
  - Validado: `tests/Unit/AccountGovernanceIntegrationServiceTest.php`, `tests/Unit/AccountGovernanceServiceTest.php`, `tests/Unit/Services/AccountGovernanceServiceTest.php`, `tests/Unit/Services/ItemServiceTest.php` (19 testes) + suíte completa `Unit` (4428 testes, 0 falhas) passando após o fix.
- **Bug 3 (achado ao investigar o Bug 2 em produção) — `MercadoLivreClient::getMultiItemVisits()` sempre retornava 0 visitas para 100% dos itens, em todo o sistema, não só na Governance**:
  - Causa raiz: o método chamava `GET /items/visits?ids={id1,id2,...}&date_from=&date_to=` com até 50 IDs por requisição. A doc oficial da API ("Visitas") documenta que essa variante com faixa de datas só aceita **1 ID por chamada** — com mais de um, a API sempre responde `400 validation_parameters "maximum amount of items to query is 1"`. Confirmado batendo direto na API real (conta 1335) antes do fix: toda chamada com 2+ IDs retornava esse erro, e o código caía no fallback silencioso que zera `visits=0` para o lote inteiro. Isso explica por que `total_visits_30d` sempre aparecia 0 no Account Governance mesmo com vendas reais (48 vendas em 30d, 0 visitas) — e o mesmo bug afeta `AccountXRayService::enrichWithMetrics()` e `MercadoLivre\SEOMetricsCollectorService`, os outros 2 consumidores deste método.
  - Fix: reescrito para chamar `GET /items/{id}/visits/time_window?last={days}&unit=day` (1 requisição por item — essa variante aceita 1 ID mas já retorna detalhamento diário em `results`), com `usleep(30ms)` entre chamadas para não estourar rate limit.
  - Validado contra a API real da conta 1335: itens com vendas confirmadas (`MLB4439459663`, `MLB6574414098`, etc.) agora retornam visitas reais e coerentes (299, 1596, ...) em vez de 0 fixo.
  - Validado end-to-end em produção via chamada real ao endpoint `/api/account-governance/diagnostic-ml` (183 itens da conta 1335): antes do fix 3, `visits_30d=0` para 100% dos itens; depois, 109/183 itens com tráfego real detectado, classificação realista (`MORTO: 130, FRACO: 25, SAUDAVEL: 11, POLUIDOR: 14, EM_RISCO: 2, TOXICO: 1`) em vez do `FALLING` indiscriminado do Bug 2. Tempo de resposta: 61,6s para 183 itens (1 chamada de visitas por item) — HTTP 200, sem timeout, mas é um aumento real de latência frente ao comportamento anterior (que era rápido só porque falhava sempre). **Atenção**: contas com mais itens (limite atual `max_items=200`) podem chegar a ~70-75s; se algum proxy/CDN na frente tiver timeout mais agressivo que isso, vale revisar (não foi possível inspecionar config de nginx/apache no ambiente, sem permissão de leitura).
  - Teste de contrato `tests/Unit/MLVisitsContractTest.php::testGetMultiItemVisitsReturnsMapIndexedByItemId` atualizado para o novo formato de chamada (mock por endpoint `/items/{id}/visits/time_window` em vez do formato antigo batched); adicionado `testGetMultiItemVisitsNeverBatchesMultipleIdsInSingleCall` como regressão específica para este bug.
  - Suíte completa `Unit` revalidada após este fix: 4429 testes, 0 falhas.

## 2026-08-09 (4ª parte) — Correção de metodologia SEO: "volume de busca" fabricado em 6 serviços (autocomplete/oferta/vendas ≠ demanda real)

- **Contexto**: dono trouxe glossário de SEO para marketplace (Search Intent, Keyword Research, Search Engine Mechanics) com alerta explícito: "Autocomplete não é sinônimo de volume de busca" e que o ML não expõe volume de busca via API pública. Auditoria encontrou 6 pontos do código fabricando ou rotulando errado esse número como se fosse demanda real medida.
- **Achados e fixes** (todos com `php -l` + `ReadLints` limpos, sem tabela/coluna de banco alterada — só chaves de array/JSON em memória):
  - `TechSheetService::estimateVolumeFromPosition()` — calculava um "volume" fake (base 10.000, decaindo 10%/posição) a partir da posição em autocomplete/trends. Renomeado para `estimatePositionRelevanceScore()` (0-100, sem pretender ser volume absoluto); chave `search_volume`/`estimated_volume` no pipeline de sugestão de MODEL renomeada para `position_relevance_score`/`inferred_signal_score` (metadata informativa, nunca usada para ranking — confirmado que `TechSheetModelSuggestionPolicy::selectBest()` não ordena por isso).
  - `KeywordKiller::estimateVolumes()` — multiplicava `total_results` (oferta real) por 7% arbitrário e chamava de `estimated_volume`; fallback `estimateVolumeHeuristic()` fabricava volume a partir do **comprimento da palavra** quando a API falhava. `estimateCompetition()` também usava só o comprimento da keyword, ignorando o dado real (`total_listings`) já disponível na mesma função. Reescrito como `analyzeKeywordSupply()`: reporta só `total_listings` (real, via API ML) e classifica competição a partir desse mesmo dado real (`classifyCompetitionFromListings`); quando a API falha, `total_listings=null` e `competition='desconhecida'` em vez de inventar substituto.
  - `TrendsService` — `getRealSeasonalityData()`/`getAggregatedSeasonalityData()` rotulavam histórico real de vendas/pedidos como `search_volume` (dado real, só mal nomeado — implicava medição de busca quando é vendas). Renomeado para `volume_signal` em toda a cadeia interna (`analyzeSeasonality`/`estimateVolume`/`calculateYoYGrowth`/`detectSeasonalPattern`/`findPeakMonths`/`findLowMonths`). **Não tocado**: leituras de `market_keywords.search_volume` (dado real de Keyword Planner, mesma coluna usada por `GoogleKeywordPlannerService`) e pass-through do `/trends` da API ML em `formatKeywords()` — esses são legítimos.
  - `App\Services\KeywordResearchService::estimateSearchVolume()` — pior caso encontrado: `monthly_volume` era `2000 - (tamanho_da_keyword * 50)`, sem nenhuma chamada de API, 100% inventado a partir do texto. Exposto direto em `POST /api/seo/keywords/volume`. Reescrito para consultar `total_listings` real via `/sites/{site}/search` e classificar competição a partir disso; `monthly_volume` mantido no retorno só por compatibilidade de schema, sempre `null` (nunca mais fabricado). `estimateCompetition()` (também por tamanho de string) removido. Testes de integração/aceitação que checavam a chave `monthly_volume` atualizados para checar `total_listings`.
  - `App\Services\AI\Analyzers\KeywordResearchService::scoreKeyword()` — bug de double-counting: `search_volume` e `competition` eram calculados a partir do **mesmo** `$searchResults` (total de anúncios), só com escalas diferentes (/100 vs /50), e ainda entravam separados na média ponderada (0.3 + 0.2 = 50% do score vindo do mesmo número disfarçado de 2 métricas). Consolidado em `total_listings` (dado real) alimentando só `competition` (peso 0.5); pesos de `relevance`(0.3)/`commercial_intent`(0.2) mantidos.
  - `MarketAnalytics::discoverKeywordOpportunities()` — descrição da oportunidade alegava "keywords com alto volume de busca via Trends do Mercado Livre", mas a função só retorna termos de Trends + autocomplete (sem número de volume nenhum). Texto corrigido para não alegar volume: "Termos em alta e sugestões de autocomplete... relacionados às suas categorias ativas".
  - `UseContextService::getSuggestionReason()` — frases estáticas por contexto ("Alta demanda de buscas por uso profissional/delivery", "Muitos usuários buscam para viagens e passeios") alegavam evidência de busca real sem nenhum dado por trás. Reescrito para descrever fit de cenário de uso/categoria, sem alegar volume ou frequência de busca.
  - Também corrigido no mesmo escopo: card "VOLUME ESTIMADO: {valor} ou 'Alto'" em `app/Views/seo/dashboard.php` (pesquisa de keywords via IA) — o fallback literal `'Alto'` aparecia sempre que a IA não retornava `search_volume` (que nunca é pedido no prompt), ou seja, o usuário via "Alto" 100% das vezes sem base nenhuma. Trocado por "ESTRATÉGIA RECOMENDADA" usando `recommended_strategy` (campo real do JSON retornado pela IA).
  - `AISEOOptimizerService::getKeywordSearchVolume()`/`getKeywordCompetition()` — pior caso de todos: quando o Google Keyword Planner (dado real, já implementado corretamente) não estava configurado, o sistema pedia pra uma **IA generativa "chutar" um número exato** de volume de busca mensal (alucinação apresentada como estimativa), e se isso falhasse, caía num fallback heurístico por tamanho/palavras-chave-padrão (`estimateKeywordVolumeFallback`/`estimateKeywordCompetitionFallback`, mesma fabricação por comprimento de string vista nos outros serviços). Removidos os 2 fallbacks fabricados; método renomeado para `getKeywordSearchVolumeData()`, retornando `{volume: int|null, source: string}` — `search_volume` só é um número real quando `search_volume_source === 'google_keyword_planner'`, senão vem `null` explícito em vez de inventado. Competição sem dado real agora retorna `'desconhecida'` em vez de chute de IA ou heurística.
  - Consertado de brinde (achado ao rastrear consumidores da chave renomeada em `TechSheetService`): `TechSheetModelSuggestionPolicy::selectBest()` lia/gravava `search_volume` — como esse método recebe candidatos vindos direto do pipeline de `TechSheetService` (já renomeado para `inferred_signal_score` nesta mesma rodada), ficaria lendo uma chave inexistente (sempre 0) se não fosse sincronizado. Renomeado em conjunto para manter o contrato consistente entre os dois arquivos.
  - Consertado de brinde (achado ao investigar `AISEOOptimizerService`): `SEOPrompts::analyzeKeywords()` — o prompt principal (caminho normal via IA, chamado por `AISEOOptimizerService::analyzeKeywords()`) instruía a própria IA a preencher `"search_volume": 1000` no JSON de exemplo, ou seja, pedia explicitamente pra IA inventar um número de buscas mensais que ela não tem como saber, e o parser (`parseKeywordsAnalysisResponse`) repassava esse valor sem nenhuma validação. Prompt reescrito para pedir `"estimated_interest": "alto|medio|baixo"` (juízo qualitativo) em vez de um número, com instrução explícita proibindo `search_volume`; e adicionada defesa extra no parser removendo qualquer `search_volume` que a IA inclua mesmo assim (LLMs nem sempre seguem instrução à risca).
- **Não alterado (confirmado legítimo)**: `GoogleKeywordPlannerService` (lê `market_keywords.search_volume`, dado real de rotina de Keyword Planner); `KeywordSourceService`/`LongTailGeneratorService` (já usavam `frequency`/relevance score corretamente rotulados, incluindo o high/medium/low por word-count do long-tail, que é heurística de SEO padrão da indústria, não uma métrica fabricada com falsa precisão numérica); scores de "volume" em `pricing/dashboard*.php` (unidades vendidas estimadas por elasticidade de preço — métrica diferente, não é volume de busca).
- Validado com suíte PHPUnit completa: `--testsuite Unit` 4428/4428 OK (27 skipped, pré-existentes, sem falhas) rodada 3x ao longo da sessão (após cada leva de fixes) após corrigir 1 teste de estrutura (`RealIntegrationsStructureTest::testKeywordKillerHasVolumeEstimation`) que checava os métodos antigos removidos. `php -l` + `ReadLints` limpos em todos os 12 arquivos tocados (8 services + 1 policy + 1 prompt + 1 view + 3 arquivos de teste).
- **Pendente de decisão do dono (inalterado desta rodada)**: nenhum item novo; itens já pendentes de sessões anteriores (unificação AutoPilot, cron de AutoPilot, destino do PredictiveAnalyticsService) continuam em aberto.

## 2026-08-09 (3ª parte) — Correção de metodologia SEO: "KEYWORDS oculto" não é recurso real do ML + MODEL não deve receber keyword stuffing

- **Contexto**: dono trouxe glossário de metodologia de agência (Marketplace SEO) com um alerta explícito: "palavras-chave ocultas"/"campo secreto"/"backend keywords" não devem ser tratados como recursos comprovados do Mercado Livre; usar "sinal derivado/inferido", nunca "campo oculto confirmado". E para o campo MODEL: "normalização real, nunca depósito de palavras-chave".
- **Achado 1 (confirma o alerta empiricamente)**: `HiddenFieldsService` (E2, usado por `SEOKillerController`/`TechSheetSEOIntegrationService`) tratava `KEYWORDS` como atributo oficial do ML com **peso de 50% no optimization_score** (o maior peso de todos). Testei ao vivo contra `GET /categories/{id}/attributes` da API real em 5 categorias do catálogo: `KEYWORDS` **não existe em nenhuma** (0/5); `MPN`/`GTIN` existem em 100%; `LINE` em 60%; `ALPHANUMERIC_MODEL` em só 20%. Ou seja, metade do score de "campos ocultos" era atribuída a um campo inexistente, e o endpoint `POST /api/seo-killer/strategies/hidden-fields/apply/{itemId}` tentava gravar isso via `PUT /items/{id}` **sem nenhum SafetyGuard**.
- **Achado 2**: `KeywordInjectorService::injectInModel()` (E3, alcançável só via dispatcher interno `optimizeText(fieldType='model')`, hoje sem nenhum caller em produção — código morto, mas filosoficamente incorreto) anexava até 5 keywords soltas ao final do valor de MODEL, contrariando diretamente `TechSheetModelSuggestionPolicy` (Ficha Técnica), que é disciplinada e rejeita stuffing.
- **Fix 1 — `HiddenFieldsService`**: `analyzeItem()`/`generateSuggestions()`/`applyToItem()` agora validam cada campo contra o schema real da categoria (`MercadoLivreClient::getCategoryAttributes()`, cache 12h) antes de tratá-lo como aplicável — campo não confirmado no schema é reportado em `not_applicable_in_category` com o motivo, nunca entra no score nem é aplicado. `KEYWORDS` passa a ser sempre rejeitado (não existe no ML); `LINE`/`ALPHANUMERIC_MODEL` só entram quando a categoria realmente os suporta. `applyToItem()` ganhou também `SafetyGuard` (mesmo padrão dos outros 3 pontos corrigidos hoje).
- **Fix 2 — `KeywordInjectorService::injectInModel()`**: desativado (marcado `@deprecated`), agora retorna o valor original inalterado com `skip_reason: model_keyword_injection_disabled`, orientando a usar `TechSheetModelSuggestionPolicy`. Assinatura mantida (zero breaking change).
- **Validado em runtime real** (não só sintaxe): categoria MLB1641 (sem LINE/KEYWORDS/ALPHANUMERIC_MODEL) → sugestões geradas só para MPN/GTIN, resto listado em `not_applicable_in_category`; categoria MLB186144 (com LINE/ALPHANUMERIC_MODEL) → sugestões geradas para os 4 campos reais, só KEYWORDS rejeitado. Bloqueio de conta 1335 em `applyToItem()` confirmado com stub (PUT nunca chamado). `injectInModel()` confirmado retornando valor original sem stuffing. `php -l` + `ReadLints` limpos; suítes `HiddenFieldsServiceTest`/`AdditionalStrategiesTest` (7/7) e `TechSheetSEOIntegrationTest` (20/20) OK.

## 2026-08-09 (2ª parte) — GAP DE GOVERNANÇA corrigido na Ficha Técnica: 3 pontos de apply sem SafetyGuard

- **Achado (via investigação "o que o módulo Ficha Técnica faz")**: `TechSheetService::applyApproved()` — chamado direto pela UI (`TechnicalSheetController`), pelo batch (`TechSheetBatchOptimizerService`), por jobs (`JobService`) e pelo auto-optimizer (`TechSheetAutoOptimizerService`) — fazia `PUT /items/{id}` na API ML **sem checar SAFE_MODE/FORBIDDEN_ACCOUNTS**, ao contrário de `HiddenSeoSuggester::applyPending()` e do CLI `tech-sheet apply`, que sempre usam `SafetyGuard`. Investigando mais a fundo, achei o mesmo gap em `TechSheetSEOIntegrationService::applyOptimizedTitle()` e `applyOptimizedDescription()` (aplicam título/descrição otimizados direto no ML, usados pela UI, API routes e pelo `bin/bulk-seo-worker.php`/`BulkSEOService`) — nenhum dos dois tinha qualquer guard de conta.
- **Fix**: reutilizado `App\Services\HiddenSeo\SafetyGuard` (sem capability nova) nos 3 pontos: `TechSheetService::applyApproved()` (protege automaticamente todos os 4 callers acima, já que todos passam por esse método único), `TechSheetSEOIntegrationService::applyOptimizedTitle()` e `::applyOptimizedDescription()`. `allowApply=true` nos 3 (chegar nesses métodos já significa aprovação/pedido explícito do usuário); `FORBIDDEN_ACCOUNTS` (1335) bloqueado de forma incondicional em todos.
- Validado com bloqueio real simulado (conta 1335 + mock de `MercadoLivreClient`): os 3 pontos retornam erro `UnsafeOperationException` **antes** de qualquer `put()` na API (confirmado que o stub de `put()` nunca é chamado). Suítes `TechSheetServiceTest`/`HiddenSeoSuggesterTest`/`TechSheetHiddenSuggestionServiceTest`/`TechSheetGtinSuggestionServiceTest` (16/16) e `TechSheetSEOIntegrationServiceApplyTest` (4/4) OK sem regressão. `php -l` + `ReadLints` limpos. Suite Unit completa: 4428/4428, 0 falhas.
- **Nota sobre o módulo em si** (explicado ao dono antes do fix): Ficha Técnica funciona como completador governado da identidade técnica do anúncio (preenche MODEL/GTIN/LINE/MPN/etc. com evidência do título, item irmão ou benchmark de concorrentes, nunca inventa GTIN). O ML mede isso oficialmente via `/items/{id}/performance` (tags `TECHNICAL_SPECIFICATIONS`/`GTIN`), mas o repositório ainda não tem métrica automática ligando um apply a ganho real de impressões/ranking/vendas — hoje a observação pós-apply (48-72h) é manual.

## 2026-08-09 — 2 bugs críticos do relatório de maturidade SEO Killer corrigidos: estado duplicado do AutoPilot + score array vs número

- **AutoPilot com 2 tabelas de estado desincronizadas**: `SEOKillerController::enableAutopilot/disableAutopilot/saveAutopilotConfig` escrevem em `AutoPilot` (tabela `seo_autopilot_config`), mas o badge de status da dashboard (`GET /api/seo-killer/autopilot/status`, usado pelo JS em `seo-killer.js:633/657`) lê de `AutoPilotStatusManager` (tabela `autopilot_config`) — tabelas completamente distintas. Resultado: usuário clicava "ativar", a config real mudava, mas a UI continuava mostrando "desativado" (ou vice-versa), sem qualquer erro visível.
  - **Fix**: novo método `AutoPilotStatusManager::setEnabled(bool)` (upsert só da flag `enabled`, com fail-soft/log em caso de erro) chamado a partir de `AutoPilot::saveConfig()` (cobre `enable()`/`disable()`/`saveAutopilotConfig`). Não migra schema, não decide qual tabela é "a certa" — só mantém as duas em sincronia no que importa para a UI. Unificação completa das tabelas continua pendente de decisão do dono (schemas divergem: budget/success_rate só existem em `autopilot_config`).
  - `php -l` limpo nos 2 arquivos; suíte Unit (78 testes carregados no filtro, 4400+ no total) sem regressão.
- **`IntelligentAutoOptimizer`/`AdvancedSEOMaximizer`: `array - array` fatal (TypeError)**: `AdvancedSEOMaximizer::calculateSEOScore()` sempre retornou um **array** (`overall/title/description/attributes/images/price`), mas `score_before`/`score_after` eram tratados como número escalar em 3 lugares: `AdvancedSEOMaximizer::generateImprovements()` (linha ~1238), `IntelligentAutoOptimizer::optimizeSingleItem()` (linha 129) e `IntelligentAutoOptimizer::makeOptimizationDecision()` (linha 183). A subtração `array - array` lança `TypeError` (confirmado isolado: `Unsupported operand types: array - array`), que é `\Error`, não `\Exception` — o `catch (\Exception $e)` em `optimizeSingleItem`/`optimizeSingleItem loop` **não captura**, matando o batch inteiro de auto-otimização no primeiro item que melhorou o score.
  - **Fix**: as 3 comparações/subtrações agora usam `['overall'] ?? 0` (score agregado 0-100) em vez do array cru. Também descoberto e corrigido de brinde: `AdvancedSEOMaximizer::saveOptimizationAnalysis()` fazia INSERT numa tabela `seo_optimization_analysis` **que nunca existiu no schema** (confirmado via `SHOW TABLES`) — todo INSERT falhava silenciosamente (engolido pelo `catch(\Throwable)` de `maximizeItemSEO`), e além disso tentava bindar arrays como parâmetro escalar de PDO. Adicionado `CREATE TABLE IF NOT EXISTS` (colunas `score_before`/`score_after`/`optimizations` como JSON) + `json_encode()` nos 3 campos antes do bind.
  - Validado com teste isolado da expressão (`TypeError` reproduzido old vs fix OK) e execução real de `maximizeItemSEO()` (score_before agora sempre array, sem mais fatal na subtração). **Nota**: durante o teste real surgiu um bug pré-existente e não relacionado — `getCategoryInfo()` consulta uma tabela `categories` que também não existe no schema (`meli`) — não foi tocado, fora do escopo desta rodada.
  - `php -l` limpo, `ReadLints` sem erros, suíte PHPUnit Unit: 78/78 OK (sem regressão).
- **Tabela `categories` ausente corrigida (2 pontos)**: `AdvancedSEOMaximizer::getCategoryInfo()` fazia `SELECT * FROM categories` numa tabela que nunca existiu no schema `meli` (confirmado via `SHOW TABLES`) — sempre retornava `[]` silenciosamente, degradando `optimizeTitle`/`optimizeDescription`/`optimizeAttributes`/`scorePrice` (contexto de categoria sempre vazio). `AdvancedReportController::getSalesByCategory()` tinha o mesmo problema via `LEFT JOIN categories c ON i.category_id = c.ml_category_id`, engolido por `catch(\PDOException)` — o widget "vendas por categoria" do relatório sempre voltava vazio.
  - **Fix**: `AdvancedSEOMaximizer` agora usa `MercadoLivreClient::getCategory()` (endpoint `/categories/{id}` já com cache de 24h em arquivo/Redis, método já existente e usado em 14+ lugares do código) + memoização em memória por request; nenhuma tabela nova criada. `AdvancedReportController` teve o `JOIN` removido e passou a usar `items.category_name` (coluna já denormalizada e populada a partir da API ML), eliminando a dependência da tabela inexistente.
  - Validado em runtime real: `maximizeItemSEO()` agora completa sem erro (score_before/score_after como arrays completos, sem mais "Table categories doesn't exist"); persistência em `seo_optimization_analysis` confirmada (insert + select + cleanup); query de `getSalesByCategory` testada direto no banco retornando 6 categorias agregadas (antes: sempre `[]`). `php -l` limpo, `ReadLints` sem erros, suíte `AdvancedReportControllerTest`+`AdvancedSEOMaximizerTest` 6/6 OK.
- **Pendente de decisão do dono (inalterado)**: unificação definitiva das 2 tabelas do AutoPilot; destino do `PredictiveAnalyticsService` (retomar ou remover); ativação de cron/worker para `AutoPilot::run()` autônomo (capability nova).

## 2026-08-08 (9ª parte) — Bug crítico corrigido: DescriptionKiller quebrava BulkOptimizer/AutoPilot em cascata

- **Achado via agente de exploração** ("Mapear maturidade do módulo SEO Killer", 28 serviços mapeados): `DescriptionKiller.php` fazia `new ItemService($accountId)` sem `use App\Services\ItemService` — PHP resolvia para `App\Services\AI\SEO\ItemService` (inexistente) e lançava `Error: Class not found` em **toda** instanciação. Como `BulkOptimizer` e `AutoPilot` instanciam `DescriptionKiller` no construtor, isso quebrava em cascata: endpoint manual `/api/seo-killer/optimize`, `BulkOptimizer` (usado por bulk-apply e AutoPilot) e `AutoPilot::run()` inteiro — ou seja, a otimização de descrição/bulk/autopilot estava 100% inoperante em produção.
- **Fix**: adicionado `use App\Services\ItemService;` em `DescriptionKiller.php` (1 linha). Validado via instanciação real (`new DescriptionKiller(null)` — antes fatal, agora OK) e suíte `SEOKillerEngineTest` + `AttributeKillerGapClassificationTest` + `KeywordKillerTest` (6/6 OK).
- **Mapeamento completo do módulo SEO Killer** (28 serviços, ~21,4k linhas): só 6/28 têm testes nominais; AutoPilot tem estado duplicado em 2 tabelas (`seo_autopilot_config` vs `autopilot_config` do `AutoPilotStatusManager` — botão pode ativar uma config enquanto o badge mostra desativado); `IntelligentAutoOptimizer`/`AdvancedSEOMaximizer` têm incompatibilidade de tipo (score array vs número) que quebra a decisão de auto-apply; `PredictiveAnalyticsService` (2023 linhas) é órfão — zero rota/UI e não instancia (faltam classes); nenhum cron/systemd ativo para SEO Killer/AutoPilot/A-B-testing/relatórios. Detalhe completo no relatório do agente [Mapear maturidade do módulo SEO Killer](06d700d1-1d23-4d03-b237-1157656f9bc1).
- **Continua pendente de decisão do dono**: (a) unificar as 2 tabelas de estado do AutoPilot; (b) corrigir contrato de score do IntelligentAutoOptimizer; (c) decidir destino do PredictiveAnalyticsService (retomar ou remover); (d) ativar cron do AutoPilot (capability nova — exige prompt escopado).

## 2026-08-08 (8ª parte) — GAP DE GOVERNANÇA corrigido: apply do SEO Killer sem SafetyGuard

- **Achado (via análise "o que falta pro SEO Killer")**: `BulkOptimizer::optimizeItem()` (usado por `AutoPilot::run()`), `AttributeKiller::fillMissingAttributes()` (chamado direto por `SEOKillerController::fillAttributes`) e `ImageKiller::updateImages()` faziam `PUT /items/{id}` na API ML **sem checar SAFE_MODE/FORBIDDEN_ACCOUNTS** — ao contrário do stack Hidden SEO/Ficha Técnica, que sempre usa `SafetyGuard`. Ou seja: habilitar AutoPilot com `auto_apply=true`, ou chamar os endpoints de apply de atributos/imagens do SEO Killer, escreveria na conta 1335 (produção FACILYTY) sem aprovação item a item — violando o north_star do `project-status.json`.
- **Fix**: reutilizado `App\Services\HiddenSeo\SafetyGuard` (sem criar capability nova) nos 3 pontos de escrita: `BulkOptimizer::optimizeItem()` (gate único `$canApply` cobrindo title/description/attributes/update), `AttributeKiller::fillMissingAttributes()`, `ImageKiller::updateImages()`. Conta 1335 agora bloqueada nesses caminhos como já era no Hidden SEO.
- Testes existentes (`AttributeKillerGapClassificationTest` 4/4, suíte SafetyGuard 5/5) OK após a mudança. `php -l` limpo nos 3 arquivos.
- **Ainda não implementado (decisão do dono)**: AutoPilot não tem cron/worker — `enable()`/`run()` só executam por chamada manual da API/UI, mesmo com "habilitado". Ativar execução autônoma seria capability nova (exige prompt escopado).

## 2026-08-08 (7ª parte) — Repricing Fase 1 parcial em staging (aprovada pelo dono)

- Deploy staging (`scripts/deploy_staging.sh`) com o código F0; isolamento Redis DB1/MySQL eskill_staging OK.
- 5 fixtures (MLBSTG00000001..05) marcados `auto_reprice=1` + `cost_price` (80%) + `min_price`/`max_price`.
- Simulação validou guards em camadas: 1º `min_price_nao_definido` → (corrigido) → 2º `market_min_invalido` (search ML via staging retorna 0 — sem OAuth da conta 1336). Zero escrita, degradação graceful correta.
- **Bloqueio F1 end-to-end**: OAuth ML staging não conectado (pré-req humano #3 de docs/ops/STAGING.md). Com OAuth, re-rodar `bin/reprice-simulate.php --account=1336` deve produzir sugestões reais.

## 2026-08-08 (6ª parte) — Repricing Fase 0 implementada (simulação read-only)

- `RepriceSimulationService` + CLI `bin/reprice-simulate.php --account=ID [--limit=N] [--json]`: calcula preço sugerido pela regra pura extraída do SniperAgent (`computeTargetPrice`/`requiresPriceChange` — comportamento idêntico, SniperAgentDryRunTest 2/2), sem escrever nada.
- Tetos via env (`.env.example`): `REPRICE_MAX_PCT=5`, `REPRICE_MAX_ITEMS_PER_RUN=5`, `REPRICE_MIN_MARGIN_PCT=10`. Violação = skip com motivo, nunca clamp.
- Conta proibida (1335): simula, mas tudo `seria_aplicado=false` + `conta_proibida`.
- Dry-run real 1335: 0 candidatos (nenhum item com `auto_reprice=1` em conta nenhuma — opt-in nunca ligado). F0 só produz relatório útil após Fase 1 marcar itens em staging.
- 11 testes novos (SQLite in-memory, zero rede). Suite Unit: 4428 testes, 0 falhas. Commit `7feee073`.

## 2026-08-08 (5ª parte) — MARCO: primeiro apply governado em produção (1335)

- Dono aprovou o lote EGR. Ritual executado: `approve --attribute=EMPTY_GTIN_REASON --limit=5` → revertido MLB4564086941 para pending (tinha EGR="Outro motivo"; princípio conservador: só preencher lacuna, nunca sobrescrever valor existente sem decisão explícita) → `apply --dry-run` (4 itens) → `apply --i-understand-prod`.
- **Aplicados 4/4, 0 erros**: MLB4564086551, MLB6574546320, MLB6574499014, MLB6574452378 → EMPTY_GTIN_REASON="O produto não tem código cadastrado" (value_id 17055160). Verificado via GET /items: todos active com o atributo correto.
- Observação 48–72h: moderação/quality no Pregão + `bin/tech-sheet performance` nos 4 itens.
- Prompt repricing **aprovado** (frontmatter atualizado); Fase 0 (simulação read-only) em implementação.

## 2026-08-08 (4ª parte) — Lote-5 EGR revisado + prompt repricing + ritual apply ensaiado

- Relatório read-only do lote-5 EMPTY_GTIN na 1335: `storage/reports/revisao-lote5-empty-gtin-1335-20260808-203749.json`. 4/5 aptos (GTIN+EGR ausentes); **MLB4564086941 já tem EGR="Outro motivo"** — apply mudaria valor, decisão do dono.
- Bug: `bin/tech-sheet approve` usava coluna `notes` inexistente (SQL error no ritual) — corrigido; novo filtro `--attribute` (min-confidence=85 pegava MODEL 95%). Dry-run valida exatamente os 5 itens revisados. Commit `485a1218`.
- Prompt escopado repricing em `.github/prompts/repricing-automatico.prompt.md` — **draft, aguarda aprovação** (F0 simulação → F1 staging → F2 prod com tetos ±5%/5 itens/1x-dia).
- Staging `--check` verde (OAuth ML staging ainda pendente — pré-req humano). E2E readonly prod: 15/15. TechSheet suite: 88/88.
- **Nada aplicado no ML 1335.** Ritual quando aprovado: `approve --account=1335 --attribute=EMPTY_GTIN_REASON --limit=5` → `apply --account=1335 --limit=5 --dry-run` → `apply ... --i-understand-prod` → observar 48-72h.

## 2026-08-08 (3ª parte) — Stash recuperado + harness 0 avisos

- Stash `wip-pre-recovery` (05/08) recuperado e commitado (`595d7aa3`): logging fail-soft (sem 500 em request quando log bloqueado) + chmod 0660 (ACL eskill/root). Stash dropado após commit.
- `.hermes/` (artefatos de agente) → quarentena. Duplicidade scheduler.php root×eskill é inofensiva (lock em /tmp).
- Validação final: Unit 4417 testes 0 falhas; `bin/init.sh` → **"Sistema pronto — 0 avisos"**, project-status 23/25 (92%).

## 2026-08-08 (2ª parte) — Logs de teste isolados + project-status.json restaurado

- `LogService` respeita `LOG_DIR`; `tests/bootstrap.php` aponta `LOG_DIR`/`LOG_PATH` → `storage/logs/tests/` (gitignored). Produção sem poluição (delta zero em app.log/llm_error.log).
- `LogTest::testErrorLogsToErrorFile` derivava path hardcoded de prod → agora usa `Log::channel('app')->getLogDirectory()`.
- `project-status.json` restaurado (ausente desde mai/2026): 25 features, 23 passando; falhando = `repricing-automatico` (OFF) e `apply-ml-1335-tech-sheet` (aguarda aprovação).
- Lixo (~800KB) em `.tmp/quarantine-2026-08-08/` (reversível).
- Suíte Unit: 4417 testes, 10.441 assertions, 0 falhas, 27 skips. Commit `455b23eb`.

## 2026-08-08 — Inspeção geral + commits do backlog + fixes de infra

- **Backlog commitado**: 18 commits (7a3fb346..678bdb0c), 223 arquivos, ~37,7k inserções. Grupos: auth/TOTP+QR, token refresh, financial ledger, central de Vendas, Hidden SEO/MODEL/GTIN, Pregão QA, pricing-v2, webhook hardening, ads/claims/brand-search/items/seo-killer, dashboard, TestSprite. Lint `php -l` OK em 171 PHPs. Lixo excluído (.cursor, .codacy/generated, __pycache__).
- **Crontab root limpo**: 10 entradas mortas comentadas (9 scripts inexistentes + `run_repricer.php` com fatal 100% — classe `App\Services\RepricingService` nunca existiu no git). Backup em `/root/crontab-root-backup-20260808.txt`. Funções vivas seguem no crontab do user `eskill`. **Repricing automático está OFF** (SniperAgent só roda manual via AgentController; nada o agenda) — decisão de substituto pendente do dono.
- **pregao-qa-worker**: flap infinito corrigido — `Restart=always` → `on-failure` + novo `pregao-qa-worker.timer` (60s). Worker é one-shot por design (sai 0 com fila vazia). Verificado: NRestarts=0, 2 ciclos limpos. Units em `config/systemd/` sync com /etc/systemd/system.
- **Vazamento Gemini→API real corrigido**: `tests/bootstrap.php` injeta keys fake; `AIIntegrationTest::skipIfNoEnvVars()` só checava isset → rodava integração com key fake contra Google real (400s em llm_error.log). Guard agora rejeita `phpunit-fake-key-not-real`. Verificado: 3 skips, zero rede. Demais testes suspeitos não vazam.
- **Falsos alarmes confirmados**: "fatais" do `MlAdvancedFeaturesService` no app.log = poluição do próprio unit test (mocks com msgs "fatal"/"ML API down"); ledger tick `created=0` = idempotência (scanned=14, billing=29, R$900,11 pending release — saudável); falhas de `ml_webhook` (23k) = históricas (RetryService inexistente + 401 no proxy CF), últimas 24h: 215 completed / 0 failed.
- **Dívida anotada**: logging de testes escreve nos logs de produção (app.log/llm_error.log); `project-status.json` segue inexistente (init.sh avisa).

## 2026-08-07 — MCP ML/MP: performance API + boolean value_id

- MCP ML autenticado: docs `atributos` + `qualidade-das-publicacoes` (/health → /performance).
- MCP MP: 0 applications na conta OAuth — sem criar app (exige confirmação explícita).
- `ItemPerformanceService` + CLI `php bin/tech-sheet performance` (live MLB6526496096 score=72).
- Hidden SEO / apply: meta `value_id`+`value_type`; boolean sem value_id é skip (doc oficial).
- PHPUnit ItemPerformance 3/3. Sem apply ML 1335.

## 2026-08-07 — Hidden SEO schema + env + staging persist

- `.env` prod+staging: `SAFE_MODE=true`, `FORBIDDEN_ACCOUNTS=1335`, `MAX_ITEMS_PER_RUN=500`.
- `php scripts/apply_hidden_seo_schema.php`: índice + view `v_pending_hidden_seo_changes` (meli + eskill_staging).
- Staging 1336: `--persist` → 10 pending na view (title + same_item). Sem apply ML 1335.

## 2026-08-07 — Hidden SEO Gap Fixer (SafetyGuard + VOs + dry-run default)

- Assunção: NÃO greenfield `src/` (colide monorepo `App\`); integrou no stack Ficha Técnica.
- `SafetyGuard` + `UnsafeOperationException`; SAFE_MODE/FORBIDDEN_ACCOUNTS/MAX_ITEMS_PER_RUN no `.env.example`.
- VOs: Mpn/Line/Evidence/SuggestionSource; Entities Gap/Suggestion/PendingChange; TechSheetMapper + HiddenSeoSuggester.
- CLI `suggest-hidden`: dry-run padrão; `--persist` grava pending; `--apply` ML bloqueado em 1335.
- `apply` CLI: blacklist 1335 via SafetyGuard; override só com `--i-understand-prod`.
- Schema: view `v_pending_hidden_seo_changes` sobre `tech_sheet_suggestions`.
- PHPUnit Hidden SEO + SafetyGuard/Mapper/VOs: 56 assertions suite filter OK. Sem apply 1335.

## 2026-08-07 — Hidden SEO Ficha Técnica (prompt aprovado + implementado)

- `TechSheetHiddenSuggestionService`: evidência title → same_item → local_catalog; nunca inventa.
- Prioridade LINE/MPN/HANDLE_RISER; skip MODEL/GTIN/ops; variation_attribute+variações = skip.
- Hook em `generateSuggestions` + CLI `php bin/tech-sheet suggest-hidden`.
- UI: label origem “Hidden SEO” + banner se ≥10 hidden gaps (seo-killer + tech-sheet).
- Staging 1336: MPN pending com source=hidden_seo; idempotente OK. PHPUnit 14/14.
- Sem apply ML 1335. Sem commit.

## 2026-08-07 — EMPTY GTIN batch 1335 + UI labels + prompt Hidden

- Geradas **18** sugestões `EMPTY_GTIN_REASON` pending (DB local; sem apply ML). Saldo EAN=0.
- UI: labels “Estratégia de busca” / “Guarda-chuva multi” / “Motivo sem GTIN”.
- Novo prompt aguardando OK: `.github/prompts/ficha-tecnica-hidden-seo.prompt.md`.
- HAS_COMPATIBILITIES é read_only no ML → não sugerido.
- Sem apply. Sem commit.

## 2026-08-07 — MODEL umbrella multi-compatível + CLI suggest-model

- Títulos CG/Titan/Fan/Start + cilindrada → MODEL guarda-chuva (`CG 160`), `meta.policy=umbrella_multi_compatible`.
- CLI: `php bin/tech-sheet suggest-model --account=X [--dry-run]` (1335 exige `--i-understand-prod`).
- Staging: MLBSTG00000004 → CG 160. PHPUnit 12/12. Sem apply ML. Sem commit.

## 2026-08-07 — MODEL policy v2 (ano/Fan160/multi-modelo)

- Rejeita ano isolado (`2025`); `Fan160`→`Fan 160` (sem quebrar `F800`).
- Skip generate se título ambíguo (≥3 linhas: Titan/Fan/Start/CG…).
- Sanitize local 1335: 1 rejected, Fan 160 + CB 300 normalizados; F800 revertido.
- Staging: multi skip + Fan 125 ok. PHPUnit 10/10. Instrumentação debug removida após verify.
- Sem apply ML. Sem commit.

## 2026-08-07 — MODEL mapeamento semântico (prompt aprovado + implementado)

- `TechSheetModelSuggestionPolicy`: limpa identificador, rejeita stuffing, mining só sobe score.
- Hook em `generateModelSuggestions` + generate título para attrs MODEL.
- Staging 1336: Fan 125 / Titan 150 / Biz 125 com `meta.policy=semantic_field_mapping`.
- PHPUnit `TechSheetModelSuggestionPolicyTest` 7/7. Instrumentação debug removida após verify.
- Sem apply ML 1335. Sem commit.

## 2026-08-07 — GTIN Ficha Técnica fechado; próximo prompt MODEL

- Prompt GTIN → status `implemented`. Dry-run 1335: 4/5 EMPTY “não tem código cadastrado” (saldo EAN=0); 1 skip GTIN já presente.
- Restam 28 MODEL `pending` / 6 `applied` na 1335.
- Novo prompt (aguardando OK): `.github/prompts/ficha-tecnica-modelo-mapeamento-semantico.prompt.md` (notas de `documentacao/arm/ficha.tecnica.md`).
- Sem apply GTIN/MODEL extra em 1335. Sem commit.

## 2026-08-07 — GTIN Ficha Técnica ↔ EanService (prompt aprovado + implementado)

- Prompt aprovado pelo dono; status `approved_implementing`.
- Novo `TechSheetGtinSuggestionService`: pool EAN soft-reserve OU `EMPTY_GTIN_REASON` (nunca inventa EAN).
- Hooks: generate → GTIN; reject → libera reserva; apply OK → `confirmEanReservationAfterApply`.
- CLI `php bin/tech-sheet suggest-gtin` (prod/1335 exige `--i-understand-prod`).
- UI: banner saldo EAN + labels “EAN do pool” / “Motivo sem GTIN”.
- Fixture staging: categoria `MLB438146` (tem EMPTY); smoke conta **1336**: EMPTY “não tem código cadastrado”, idempotente OK.
- PHPUnit `TechSheetGtinSuggestionServiceTest` 9/9. Instrumentação debug removida após verify.
- Sem apply GTIN em 1335. Sem commit.

## 2026-08-07 — Prompt GTIN Ficha Técnica ↔ EanService (aguardando aprovação)

- Escrito `.github/prompts/ficha-tecnica-gtin-ean-pool.prompt.md` (sem implementação).
- Abordagem: pool GS1 via `EanService` → senão `EMPTY_GTIN_REASON`; proibido inventar EAN.
- 1335: saldo EAN=0 → só EMPTY até comprar/importar códigos.
- Sem commit.

## 2026-08-07 — Apply MODEL approved → ML (conta 1335)

- OK explícito do dono. `php bin/tech-sheet apply --account=1335`: **6/6 OK**.
- Confirmado via GET autenticado: Factor 150, Biz 125, Fazer 250×2, Pop 100, Fan 125.
- DB: 6 `applied`, 28 `pending` MODEL restantes.
- Fix CLI SQL DISTINCT+ORDER BY; `applyApproved` trata `success:false`; default `$userId=null`.
- Sem commit.

## 2026-08-07 — Ficha Técnica abas verificadas; debug removido

- Pós-verify logs: pending=123, critical=0, hidden=123, review=15 (conta 1335).
- Instrumentação `listItems` removida (prod + staging).
- Sem commit. Apply ML 1335 ainda exige OK explícito.

## 2026-08-07 — Ficha Técnica abas + login account hydrate

- Abas UI: Lacunas / Críticas / Hidden SEO / Sugestões / Concluídos / Todos (`applyListingTabFilter`; `critical` ≠ hidden-only).
- Runtime 1335: pending=123, critical=0, hidden=123, review=15.
- Login hidrata `active_ml_account_id` via `SessionHelper` (Auth + 2FA).
- Script `scripts/seed_staging_tech_sheet_fixture.php` (só staging, ≠1335).
- Sync staging. Instrumentação `listItems` ainda ativa p/ verificação UI.
- Sem commit. Apply ML 1335 ainda pendente de OK.

## 2026-08-07 — Ficha Técnica TC038 desbloqueado (staging)

- Root cause: `TechnicalSheetController` usava só `BaseController::getActiveAccountId()` (sessão); pós-login sem chave → `Nenhuma conta conectada`. Fix: `SessionHelper::getActiveAccountId() ?? getActiveAccountId()` (prod + staging).
- Seed staging: conta fixture `1336` (`STAGING_UI_999001`, ≠1335) + 25 items → paginação 2 páginas.
- TestSprite TC037–TC039 ✅; relatório `testsprite_tests/testsprite-mcp-test-report.md`.
- Instrumentação debug removida após TC038 PASSED. Apply ML em 1335 **não** executado.
- Sem commit.

## 2026-08-06 — Pregão/Ads Onda 1 + E2E readonly

- PHPUnit `--filter Pregao` 98 OK; `--filter Ads` 43 OK; JS unit 31 OK.
- Novos unit: `PregaoQaProofTest`, `PregaoQaWorkerProtocolTest`, `SkuCustoServiceTest`.
- E2E readonly: 15/15 OK. Fixes:
  - `pregao.css`: `#pregao-root .chart-empty[hidden] { display:none !important }`.
  - `mutation-guard`: continue em non-http / document / loopback (evita `route.fetch` vs php -S).
  - `pregao-overflow`: CSS via `addStyleTag({ content })`.
  - `playwright.config`: `workers: 1` quando `E2E_READONLY=1`.
  - `package.json` `test:e2e:readonly`: remove `TMPDIR=.tmp` (Chromium crash/timeout em setContent).
- Sem commit.

## 2026-08-06 — Financial coverage restante (ingestion/PnL/settlement)

- Novos unit tests: BillingChargeIngestion, FinancialIngestion, SettlementIngestion, WithdrawalIngestion, FinancialReconciliationService, PnlReportService, SettlementReportService.
- Ledger `final` → instância real + PDO mock (sem double).
- `tests/Unit/Services/Financial/` → **146 OK** / 482 asserts.
- Sem commit.

## 2026-08-06 — Corrigir tudo possível (Unit PHP + JS + Financial coverage)

- **Unit PHP full:** 4342 OK (0 fail; era 2). Fixes:
  - `PregaoStreamServiceAgentEventFilterTest`: assert assinatura com `$qaProof, $qaRuns`.
  - `AdsServiceTest::testGetAdsItemsFalhaNaPaginaTardia…`: `incomplete=false` em HTTP mid-page (fail-closed canônico).
- **JS unit:** 31/31 OK (era 4 fail). `pregao-snapshot-state.test.js` alinhado a `pregao-qa.js` (EMPTY/PROJECTION keys, `/qa/live/{uuid}`, label APROVADO, load PregaoQaUi no sandbox, cache `pregao.js?v=45`).
- **Financial coverage nova:** CustomerPaymentMethod, Subscription, FinancialForecast, ProductProfitability unit tests → pasta Financial **124 OK**.
- **Ainda sem unit (P3 / I/O pesado):** BillingChargeIngestion, FinancialIngestion, Settlement*, WithdrawalIngestion, FinancialReconciliationService, PnlReportService, SettlementReportService.
- Sem commit.

## 2026-08-06 — Financeiro fechamento (smokes + gaps)

- Smokes: `smoke_financials_dashboard` 112 PASS; `smoke_sales_pnl` 25 PASS (read-only, conta 1335).
- Feature FinancialReport + FinancialServiceTest: 17 OK.
- E2E readonly: allowlist só `mutation-guard` + `pregao-overflow` — **sem spec financeiro** (P3 / fora).
- Sem unit test (dívida P3): BillingChargeIngestion, CustomerPaymentMethod, FinancialForecast, FinancialIngestion, FinancialReconciliationService, PnlReport, ProductProfitability, Settlement*, Subscription, WithdrawalIngestion.
- Módulo Financeiro: sem falha P0/P1 aberta. Sem commit.

## 2026-08-06 — Financeiro Onda 1–3 (PaymentRefund chargebacks)

- **Escopo:** módulo Financial (PHPUnit unit + `--filter Financial` + Feature report).
- **Onda 1:** 2 falhas em `PaymentRefundServiceTest` (chargeback success + exception).
- **Causa:** testes mockavam `MercadoLivreClient::get`; produção usa `mpGet` → cliente Mercado Pago (`HasFinancialDependencies`). Success batia API real / envelope sem `id`; exception esperava throw que `mpRequest` engole.
- **Fix:** injetar mock de `mpClient` via Reflection; alinhar asserts ao envelope de erro.
- **Verde:** `phpunit tests/Unit/Services/Financial/` 106 OK; `--filter Financial` 86 OK (1 skipped); Feature+FinancialServiceTest OK.
- **Sem commit** (não pedido).
