{
  "$schema": "https://raw.githubusercontent.com/TestSprite/testsprite-cli/v0.5.0/schemas/plan.schema.json",
  "projectId": "<run: testsprite project list>",
  "type": "frontend",
  "name": "Staging smoke — login e dashboard",
  "description": "Smoke FE no staging: login e carregar o dashboard sem mutar prod nem conta ML 1335.",
  "priority": "p1",
  "planSteps": [
    {
      "type": "action",
      "description": "Navigate to /login on https://staging.eskill.com.br"
    },
    {
      "type": "action",
      "description": "Sign in with the staging seeded test user credentials"
    },
    {
      "type": "assertion",
      "description": "Assert the dashboard shell loads (navigation visible, no fatal error page)"
    },
    {
      "type": "action",
      "description": "Open /dashboard/financials (or the financials route used in staging)"
    },
    {
      "type": "assertion",
      "description": "Assert the financials page renders without a 500 and shows the main report container"
    }
  ]
}
